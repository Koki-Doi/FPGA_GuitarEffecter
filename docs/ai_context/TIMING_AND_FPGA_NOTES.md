# Timing and FPGA notes

## Slack vocabulary

- **WNS** — Worst Negative Slack on setup paths. Negative means at least
  one path needs more time than the clock period allows.
- **TNS** — Total Negative Slack on setup. Sum of every failing endpoint;
  shows whether timing is "one bad path" vs. "broadly tight".
- **WHS / THS** — Hold-side equivalents. Hold violations cannot be hidden
  by lowering the clock, so they are more dangerous than setup ones.
- A negative WNS does not mean the design is broken in simulation; it
  means it may glitch at the configured clock frequency.

## Recorded baselines

| Build | WNS | TNS | Notes |
| --- | --- | --- | --- |
| **D124 RAT live-pole highpass fix (RAT now distorts) -- DEPLOYED + PL-smoked, bench PENDING (`3367d0e3`)** | **+0.619 ns** (overall) / island clk_fpga_1 **+2.449 ns** / WHS **+0.008 ns** | 0 | Realism work order steps 5 (OD) + 6 (Distortion). Measured all 6 OD + 7 Dist models (`tools/dsp_sim` harmonic_profile + net curve); EVERYTHING was on-target EXCEPT RAT, which produced THD 0.0% at EVERY drive (55..127) -- it never distorted. Root cause: `ratHighpassFrame`'s `onePoleHighpass 511 9` rounds the feedback pole to 0 (precedence: `prevOut*(511>>9)=*0` -> first difference x-prevIn), attenuating 1 kHz ~24 dB so the signal into the drive+hard-clip was tiny. Fix: inline a LIVE one-pole highpass `(prevOut*507)>>9` (~150 Hz) in `ratHighpassFrame` ONLY -- the shared `onePoleHighpass` and its other intentionally-dead-pole callers (e.g. the amp HP) are untouched. RAT now: THD 17%(drv30)->24%(55)->26%(85), odd/even +77 (op-amp+diode hard clip), rms -28.4->-12.2 (in line with other dist), net peak 4138->720 Hz (mid-forward = target), FILTER works as a treble rolloff (tilt -5.3/-10.0/-39.3 at FILTER 0/50/100), brightness preserved at FILTER 0. OD all on-target (JanRay/Klon are intentionally low-gain/transparent -- verified JanRay distorts 4.6/9.8/11.7% at drive 70/90/max, not a bug); no OD/Dist over-tuning (work order policy). Only Rat.hs changed; no GPIO/topology/stage/multiplier/Tcl/XDC/block-design/topEntity-port change. Clash regen PASS (mtime trap avoided, coef 507 in VHDL); full Vivado build PASS; route errors `0`; `write_bitstream completed`; timing fully MET (`WNS +0.619`, `TNS 0`, `WHS +0.008`, `THS 0`, `WPWS +2.845`); island clk_fpga_1 `+2.449`; D109 CDC pair clk_fpga_0<->clk `+5.683`/`+6.359` (knife-edge bound intact). In the accepted baseline band (D122 +0.614 / D123 +0.595). bypass bit-exact (sim). bit/hwh md5 `3367d0e3f86fdb5d8b5d501be1c71995` / `3b24f3f2fa4e8aa2ffc1530e80c4484d`. Deployed to PYNQ-Z2 `192.168.1.9`; 2 board sites md5-matched; PL-smoke OK (bit loaded, ADC HPF `True`). **Not ear-bench accepted yet (RAT is a tonal change -- needs board confirmation); D123 `7efd41ef` remains the canonical baseline until D124 is bench-approved. Rollback via `git checkout <D123 commit> -- hw/Pynq-Z2/bitstreams/` + redeploy.** |
| **D123 Cab per-model presence biquad (model separation) -- DEPLOYED + PL-smoked, bench-ACCEPTED, MERGED (`7efd41ef`)** | **+0.595 ns** (overall) / island clk_fpga_1 **+2.677 ns** / WHS **+0.017 ns** | 0 | Realism work order step 4 phase 3 (Cab). The step-4 measurement (`REALISM_CAB_MEASUREMENT.md`) found all three cabs peaked at the SAME 2806 Hz -- the cone-breakup presence biquad used ONE shared coeff set for every model, so there was no presence separation. Fix: per-model presence biquad (coeff-only mux in the EXISTING `cabPresenceFeedforwardFrame`/`cabPresenceRecursiveFrame`, same D82/D83 ff/rec split, like the D122 ampScoop mux). RBJ peaking 96 kHz Q14: open 1x12 3400 Hz Q0.8 +3.0 dB (brighter/airier), british 2x12 2800 Hz Q1.0 +3.5 dB (UNCHANGED, mid-forward identity), closed 4x12 2300 Hz Q1.2 +4.0 dB (lower/thicker honk). New helpers `cabPresenceFFCoeff`/`cabPresenceFBCoeff` (model=`ctrlC>>6`); b1==a1 for RBJ peaking so na1=-b1. No new GPIO/topology/stage/multiplier/Tcl/XDC/block-design/topEntity-port change. Offline A/B (`tools/dsp_sim`, `measure.py cab0/1/2`): Closed peak moved 2806->2310 Hz, Open presence gentler/higher (pres 2-4k +2.9->+2.3), British unchanged; side effect Closed rolloff -3.6->-4.2 / fizz -5.9->-6.1 toward target; bypass bit-exact. Clash regen PASS (mtime trap avoided: vhdl + component.xml M, new coeffs 30865/28637/17087/16837/12274/14834 present in VHDL); full Vivado build PASS; route errors `0`; `write_bitstream completed`; timing fully MET (`WNS +0.595`, `TNS 0`, `WHS +0.017`, `THS 0`, `WPWS +2.845`); island clk_fpga_1 `+2.677`; D109 CDC pair clk_fpga_0<->clk `+5.610`/`+6.508` (knife-edge bound intact). In the accepted baseline band (D112 +0.564 / D122 +0.614). bit/hwh md5 `7efd41ef6d0b7a88ecd8c54217ad9c23` / `06611bce8a69b5409ff91ba54c531b76`. Deployed to PYNQ-Z2 `192.168.1.9`; 3 board sites md5-matched; PL-smoke OK (new bit loaded, ADC HPF `True`, GPIOs present). **Not ear-bench accepted yet; D122 `1a295b8b` remains the canonical baseline until D123 is bench-approved. Rollback via `git checkout d07c8e9 -- hw/Pynq-Z2/bitstreams/` + redeploy.** |
| **D122 Amp per-model voicing (JC-120 flatter, Rockerverb thick low-mid, TriAmp scoop) -- DEPLOYED + smoked, bench-ACCEPTED, MERGED (`1a295b8b`)** | **+0.614 ns** (overall) / island clk_fpga_1 **+2.633 ns** / WHS **+0.016 ns** | 0 | Extends D121 voicing to the amp (user authorized "amp も含めて全部寄せて"). Coeff-only changes to the EXISTING ampScoop biquad mux: JC-120 (idx 0) -5->-2 dB @ 400 Hz (a real Jazz Chorus is SS-flat, not Fender-scooped; now distinct from Twin); Rockerverb (idx 3) flat->+3 dB @ 300 Hz (thick low-mid); TriAmp (idx 5) flat->-6 dB @ 750 Hz (modern scoop). Twin/AC30/JCM800 + the differentiator input-HP + the power-sag UNTOUCHED. Finding: the amp's cascaded always-on soft-clips partially re-fill EQ scoops (cuts survive worse than boosts) -- JC-120/Rockerverb measure clearly, TriAmp scoop subtle ~-2 dB at output, bench-confirmed audible. No new stage / GPIO / topology / Tcl / XDC / topEntity-port change. Clash regen PASS, full Vivado build PASS; route errors `0`; timing fully MET (`WNS +0.614`, `TNS 0`, `WHS +0.016`, `THS 0`, `WPWS +2.845`); island clk_fpga_1 `+2.633`. bypass bit-exact (golden 13/13 incl. new amp_rockerverb/amp_triamp + re-blessed amp_jc120; amp_jcm800 + all non-amp byte-identical = surgical). bit/hwh md5 `1a295b8be4c19f3d39b0e46268b6e801` / `eb1f70122c23f0632f2a0f7007b4610f`. Deployed to PYNQ-Z2 `192.168.1.9`; 3 board sites md5-matched; PL-smoke OK (overlay loads, ADC HPF `True`). **Bench-ACCEPTED ("合格"), merged to main; new canonical baseline (supersedes D121). Rollback to D121 via `git checkout d07c8e9 -- hw/Pynq-Z2/bitstreams/` (`9a57c50a`) or D99 via `ea6bf94`.** |
| **D121 Off-target effect voicing (OD BD-2/OCD biquads, Metal scoop gate-reuse, Cab presence biquad) -- DEPLOYED + PL-smoked, bench PROVISIONALLY accepted (`9a57c50a`)** | **+0.726 ns** (overall) / island clk_fpga_1 **+3.309 ns** / WHS **+0.012 ns** | 0 | First voicing pass on the rolled-back D99 baseline, NON-amp effects only (`Amp.hs` UNTOUCHED -- accepted amp character preserved). OD BD-2 pre-clip biquad 1500->2300 Hz (brighter); OD OCD NEW pre-clip biquad +4 dB @ 1300 Hz (was flat, upper-mid honk); Metal mid-scoop via widening the existing ~700 Hz bigMuff scoop-notch biquad gate to `bigMuffOn || metalDistortionOn` (metal runs upstream, NO new stage); Cab cone-breakup presence peak = NEW peaking biquad +3.5 dB @ 2800 Hz on the speaker-FIR output (the 15-tap FIR can't resolve a 2.8 kHz peak at 96 kHz). All four designed + verified offline with `tools/dsp_sim/measure.py` (net tone curve vs bypass) BEFORE building; bypass bit-exact (golden 11/11 incl. new od_bd2/od_ocd/dist_metal/cab); RAT left alone (bright = correct). Only DSP behaviour + 2 new internal cab Pipeline stages: no GPIO/topology/Tcl/XDC/block-design/topEntity-port change. Clash regen PASS, full Vivado build PASS; route errors `0`; timing fully MET (`WNS +0.726`, `TNS 0`, `WHS +0.012`, `THS 0`, `WPWS +2.845`); island clk_fpga_1 `+3.309` (the new cab biquad cost almost nothing). bit/hwh md5 `9a57c50ae405bce717648dc1585eaf4b` / `112be061b98ed16d5ff55eaa87fc3b85`. Deployed to PYNQ-Z2 `192.168.1.9`; 3 board sites md5-matched; PL-smoke OK (overlay loads new bit, ADC HPF `True`). **Bench accepted; MERGED to main (`--no-ff`) -- D121 is the new canonical deployed baseline (superseding D99). Rollback via `git checkout ea6bf94 -- hw/Pynq-Z2/bitstreams/` + redeploy.** |
| **D119 Amp power-sag master modulation disabled -- BUILT + file-synced, PL smoke BLOCKED (`88c265cc`)** | **+0.699 ns** (overall) / WHS **+0.013 ns** | 0 | Narrow Clash DSP behaviour fix in `AudioLab/Effects/Amp.hs` after the user reported Amp ON volume pumping and clarified JC-120 does not reproduce it. The old `ampMasterFrame` dynamic sag path modulated final master level only on tube models (`idx != 0`); D119 ignores the sag envelope at the master stage and uses stable `ctrlB` MASTER directly for every model, keeping the D112 `softClipK 4_500_000` ceiling. No GPIO/topology/Tcl/XDC/block-design/topEntity/effect-order change. Clash VHDL generation PASS, IP repackage PASS, full Vivado build PASS; route errors `0`; timing fully MET (`WNS +0.699`, `TNS 0.000`, `WHS +0.013`, `THS 0.000`, `WPWS +2.845`); bus-skew constraints all `MET` (minimum slack `+8.020 ns`). bit/hwh md5 `88c265cc925cef4673277c1b49a79a02` / `8999e51470e6f6662e5b00e66390781b`. `scripts/deploy_to_pynq.sh` completed to PYNQ-Z2 `192.168.1.9`; deploy import sanity and notebook sync passed. Programmatic Pmod I2S2 mode-2 smoke was attempted but hung with no output for over 2 minutes; after interrupt, the board was unreachable (`No route to host`, ping host unreachable, ARP incomplete). **Not confirmed loaded on FPGA, not PL-smoked, not ear-bench accepted. D112 remains accepted until D119 passes smoke and bench.** |
| **D118 Amp de-muffle constant retune -- DEPLOYED + PL-smoked, bench PENDING (`c85ada77`)** | **+0.754 ns** (overall) / WHS **+0.016 ns** | 0 | Constant-only amp voicing pass in `AudioLab/Effects/Amp.hs` after the user reported the amp still felt muffled: lower `ampModelDarken` and Drive-mode `ampPreLpfDriveDarken`, Rockerverb low-mid push reduced to +1.5 dB @ 500 Hz, TriAmp scoop reduced to -2 dB @ 750 Hz, Rockerverb/TriAmp treble trims reduced, per-model presence trims opened, and shared transformer/mid compression softened (`ampTransformerKnee=6_700_000`, `ampTransformerHfDroop=6`, transformer resonance +1 dB @ 110 Hz, `ampMidSatKnee=6_800_000`). No GPIO/topology/Tcl/XDC/block-design/topEntity/effect-order change. Clash VHDL generation PASS, IP repackage PASS, full Vivado build PASS; route errors `0`; timing fully MET (`WNS +0.754`, `TNS 0.000`, `WHS +0.016`, `THS 0.000`, `WPWS +2.845`); bus-skew constraints all `MET` (minimum slack `+8.355 ns`). bit/hwh md5 `c85ada776aa04c2501f1d21fa7d8f406` / `04361bc813afdf5194b7b4774a7eecde`. Deployed to PYNQ-Z2 `192.168.1.9`; deploy import sanity and notebook sync passed. Programmatic smoke loaded the new PL and ran Pmod I2S2 mode 2 for 2 s (`FRAME_COUNT +192271`, ~96 kHz, clocks/SDOUT alive, ADC samples observed). Current physical input clipped during smoke (`PEAK_ABS_LEFT/RIGHT=8388607`, `CLIP_COUNT=118`), so bench should lower/check input level before judging tone. **Not ear-bench accepted yet; D112 remains the accepted baseline until the user confirms audio.** |
| **D117 RAT highpass/identity retune -- DEPLOYED + PL-smoked, bench PENDING (`6dc84eaf`)** | **+0.644 ns** (overall) / WHS **+0.008 ns** | 0 | Clash/VHDL RAT pass in `AudioLab/Effects/Distortion.hs`: fixed the RAT highpass feedback precedence bug so the `505/512` pole is live at 96 kHz, then retuned only RAT constants (drive gain, clip threshold, post low-pass alpha, FILTER range). No GPIO/topology/Tcl/XDC/block-design/topEntity change. Clash VHDL generation PASS, IP repackage PASS, full Vivado build PASS; route errors `0`; timing fully MET (`WNS +0.644`, `TNS 0.000`, `WHS +0.008`, `THS 0.000`, `WPWS +2.845`); bus-skew constraints all `MET` (minimum slack `+8.042 ns`). bit/hwh md5 `6dc84eaf46d2b19df3f600474ef749b4` / `1d05499010986cbe659af779f75e31f1`. Deployed to PYNQ-Z2 `192.168.1.9`; board repo copy and overlays registry md5-matched. Programmatic smoke loaded the new PL, Pmod I2S2 tone mode showed ~96 kHz frame cadence, and RAT MMIO words matched expected packing. **Not ear-bench accepted yet; D112 remains the accepted baseline until the user confirms audio.** |
| **D114 non-amp effect constant retune -- BUILT clean, file-synced, PL smoke BLOCKED (`31c768eb`)** | **+0.601 ns** (overall) / WHS **+0.010 ns** | 0 | Constant-only pass across `AudioLab/Effects/Overdrive.hs`, `Distortion.hs`, `Cab.hs`, and `Reverb.hs`: no new GPIO/topology/stage/helper/multiplier and `block_design.tcl` untouched. Overdrive model constants and existing OD mid biquad slots were adjusted for more distinct TS9/OD-1/OCD/Centaur behaviour; Distortion leaves DS-1 unchanged and trims Clean Boost/TS/Metal/RAT constants; Cab British/Closed FIRs are slightly darker with subtler micro-mod; Reverb high-TONE damping is less dark. Clash VHDL generation PASS, IP repackage PASS, full Vivado build PASS; route errors `0`; bus-skew reports all `MET` (minimum slack `+7.989 ns`); bit/hwh md5 `31c768eb4788f31de21bd30977614361` / `e380ed637f145a6377e29e13c45a098d`. `scripts/deploy_to_pynq.sh` file-synced to PYNQ-Z2 `192.168.1.9`, and all six board bit/hwh sites md5-matched. **PL load/smoke is not complete:** `AudioLabOverlay()` timed out and the board stopped responding to ping/SSH, so D114 is not confirmed loaded on the FPGA and not ear-bench accepted. Power-cycle + one overlay load + Pmod mode-2 smoke are required before acceptance. |
| **D113 amp model-identity retune -- DEPLOYED, bench PENDING (`ed76421f`)** | **+0.743 ns** (overall) / WHS **+0.018 ns** | 0 | Constant-only amp voicing pass in `AudioLab/Effects/Amp.hs`: no new GPIO/topology/stage/multiplier. Spreads the D112 amp lineup closer to hardware identities: JC-120/Twin stay cleaner, AC30 breaks up earlier with more chime, Rockerverb gains a +3 dB 500 Hz low-mid push and thicker/darker drive, JCM800 gets more bite/presence, TriAmp gains a -3 dB 750 Hz modern scoop plus tighter high-gain/fizz control. Small shared trims restore subtle transformer bloom / HF iron softness / mid grind without returning to D97 boxiness. Clash VHDL generation PASS, IP repackage PASS, full Vivado build PASS; route errors `0`; bus-skew reports all `MET`; bit/hwh md5 `ed76421fa7a5c68c5e9e79ddae5c4526` / `b4deef57b8ceb9cada033dae8ecdcd3a`. Deployed to PYNQ-Z2 `192.168.1.9`; all six bit/hwh sites md5-matched; post-deploy smoke loaded `AudioLabOverlay()`, confirmed ADC HPF `True`, `pmod_status` present, Pmod `VERSION=0x00480001`, `MODE=2 (dsp)`, and I2S clocks/SDOUT alive. **Not ear-bench accepted yet, so D112 remains the accepted baseline until D113 is bench-approved.** |
| **D112 amp full revoicing -- bench-ACCEPTED baseline (`c1e3de50`)** | **+0.564 ns** (overall) | 0 | Builds on D109's CDC fix (which is what finally lets the DSP be rebuilt for voicing -- see the D109 row). Amp opened up: cascaded always-on soft-clip knees raised (power 3.4M->6.0M, master 3.3M->4.5M, midsat 4.0M->6.5M, respres 3.4M->5.5M, transformer 5.2M->6.5M = less compression, restored transients), post-clip LPF HF ceiling `ampPreLowpass` baseAlpha 80->140 (~6 kHz -> ~12 kHz), full treble extension (`ampTrebleGain` top-rolloff removed), transformer HF droop 3->6 (≈off), `ampModelDarken`/treble-trim minimised, asym-clip base knees 4.9M/4.35M -> 5.5M/4.9M, pre/de-emph amount 1->2; master ceiling kept protective (4.5M) so JC-120 clean does not overflow satShift7. Timing fully MET (the D109 CDC `set_max_delay` bound holds: report_cdc exception "Max Delay Datapath Only", WHS +0.007). bit md5 `c1e3de50dca946c24b1b08106f8f134c`. Deployed 5-site. **Bench (Pmod mode 2): all_off CLEAN, amp natural/open with extended top, JC-120 clean, tube models de-muffled -- user-confirmed accepted (合格).** Supersedes D98 as the accepted deployed baseline. (D110/D111 were the intermediate revoicing rebuilds toward this; D110/D111 bits not separately archived.) |
| **D109 safe-bypass knife-edge ROOT-CAUSED & FIXED (untimed audio CDC) + amp HP dead-pole fixed** | **+0.564 ns** (overall) / WHS **+0.007 ns** | 0 | **The fix that ends the D58/D60/D102-D106 "knife-edge" (every DSP rebuild since D98 corrupted all_off passthrough).** Found by opening the D98 routed DCP (no rebuild): build IS deterministic (the md5 scare was only the `.bit` timestamp; config-frame bodies byte-identical), setup+hold both MET, so NOT a timing violation. `report_cdc`: the DSP-output -> DAC path `clk_fpga_0 -> clk` is a distributed-RAM async FIFO (`axis_switch_sink` -> `i2s_to_stream` RAM, **112x CDC-13 "1-bit CDC path on a non-FD primitive"**) left untimed by `set_clock_groups -asynchronous`; any netlist change shifts placement -> the untimed write routing stretches -> passthrough corrupts with NO timing symptom. **Fix (`audio_lab.xdc`):** split the single `set_clock_groups -asynchronous` into two so `clk_fpga_0`/`clk` are no longer grouped (they become timed vs each other; all other domains stay async exactly as before), then `set_max_delay -datapath_only 10.000` both directions clk_fpga_0<->clk to bound the FIFO write under ANY placement (set_max_delay cannot override a clock-group async on the same pair, hence the split). Post-build report_cdc shows the exception flipped to "Max Delay Datapath Only" and timing MET. **Bench: all_off CLEAN on a CHANGED DSP source -- the first time ever -> DSP voicing rebuilds are now SAFE.** Also fixed `Amp.hs ampHighpass` dead-pole (`prevOut*509`shiftR`9` parsed as `prevOut*(509>>9)=*0` = first difference = bright/no-bass) -> live pole 508/512 ~120 Hz so the amp passes bass. bit `a7f18ff9` (superseded by D112). Golden D98 placement at `/tmp/d98_routed.dcp`. **This row supersedes the old "only the D98 bit is bypass-clean / do not rebuild for voicing" rule.** |
| **Refactor E + F (D104) -- REVERTED at D105 (safe-bypass P&R artifact)** | **+0.613 ns** (overall) / **+2.647 ns** (island) | 0 | **DEPLOY-REJECTED: distorted on safe-bypass; rolled back to D101. See D105.** Behaviour-preserving (NO audible change) by VHDL-equivalence proof, but the netlist-structure change perturbed P&R into a bypass artifact (D58/D60 class) -- invisible to timing/logic-equivalence, only caught by ear bench. Was merged without bench (the mistake). E: `FixedPoint.foldTap` shared folded-FIR tap kernel. F: `Distortion.hs`->`Distortion/{Common,Legacy,Pedals,Rat}.hs` + `Amp.hs`->`Amp/{Models,Clip,Tone}.hs`, re-export shims (Pipeline unchanged), code-move only. **Proven == D103**: VHDL `_types` byte-identical; numeric-constant multiset (317) + operation counts (shift_right 248 / `*` 169 / `+` 277 / `-` 190) IDENTICAL; only signal renames + source-path comments differ. Island +2.647 / fabric +0.613 / WHS +0.012 / THS 0. bit md5 `c807fb3ae7e725fa2fb48f44275b2c82`. Deployed 5-site (board matched), mode-2 smoke ~96.1 kHz, merged on the VHDL-equivalence proof. Behaviour == D103 98c6593e; rollback via `/tmp/d103_backup`. |
| **Refactor B: shared biquad kernels (D103) -- REVERTED at D105 (bypass artifact)** | **+0.657 ns** (overall) / **+2.800 ns** (island) | 0 | Behaviour-preserving (NO audible change). `biquadFf`/`biquadRec`/`biquad5` in FixedPoint dedup the 5 resonant tone biquads; Pipeline wiring + D82 ff/rec split unchanged. **Proven == D102**: VHDL `_types` byte-identical; ampScoop/odMid byte-identical; the 3 single-stage/bigMuff-rec biquads differ only by a sign-flipped y1 multiplier (same value) + named `ff` + renames. Island +2.800 / fabric +0.657 / WHS +0.009 / THS 0. Resource mapping shifted (DSP 137->139, FF 30902->28920, LUT 31618->30807 -- Vivado packing more biquad arithmetic into DSP48; behaviour-preserving, VHDL-diff proven; a larger P&R delta than D99/D102). bit md5 `98c6593e3ab5537a1e1f8b875cef2af3`. Deployed 5-site (board matched), mode-2 smoke ~96.1 kHz. Merged on the VHDL-equivalence proof (no ear bench). Behaviour == D102 b18d1477; rollback via `/tmp/d102_backup`. |
| **Refactor A (constants, KEPT) + C (pedal kernels, REVERTED at D105) (D102)** | **+0.592 ns** (overall) / **+2.670 ns** (island) | 0 | Behaviour-preserving (NO audible change). A: `audio_lab_pynq/constants.py` single source for `SAMPLE_RATE_HZ`; tools/revoice.py. C: shared `pedalDriveGain`/`distLevelRaw` kernels dedup the 6 distortion-pedal mul + 6 level stages. **Proven == D101**: VHDL `_types` byte-identical, only the 6 gain lines differ (redundant resize dropped + clean_boost U11->U12, same value); distLevelRaw zero logic diff. **Timing + util IDENTICAL to D101** (island +2.670 / fabric +0.592 / WHS +0.030 / THS 0; DSP 137, LUT 31618, BRAM 6). bit md5 `b18d147725d5dc323ecbe58bb75719da` (changed only by the resize/width tweaks). Deployed 5-site (board matched), mode-2 smoke ~96.1 kHz. Behaviour == bench-accepted D101 (no re-bench). New deployed baseline; rollback D101 `9e09ff27` via `/tmp/d101_backup`. |
| **amp/RAT input-HP pole live at higher corner (Jun 5, D101) -- REVERTED at D107 (amp muffled + too loud in full amp+cab use; rolled back to D98)** | **+0.592 ns** (overall) / **+2.670 ns** (island) | 0 | Later DEPLOY-REJECTED (D107): in full amp+cab use the pole made the amp muffled (removed D98's first-difference HF rise) + too loud (passed the lows it had cut); rolled back to D98. Enabled the previously-dead amp/RAT input-HP feedback pole (`(prevOut*coef)>>shift` in `FixedPoint.onePoleHighpass`) at a raised corner -- amp coef 502 (~298 Hz), RAT coef 505 (~209 Hz) -- to tighten input lows without D100's 90/30 Hz bass bloom, while still removing the dead-pole first-difference's +6 dB HF rise. Fully timing-clean: island clk_fpga_1 WNS +2.670 / 0 fail, fabric +0.592 / 0 fail, WHS +0.030, THS 0. DSP `137` (+2 vs D99's 135 = the two constant-multiplies for the now-live pole), LUT `31618`, BRAM `6`. bit md5 `9e09ff273b6095e0b138577c8fcca903`. Deployed 5-site (board matched); mode-2 smoke ~96.1 kHz; **bench-ACCEPTED (tighter lows, no bloom). New accepted deployed baseline, superseding D99** (`83a64ffc`, rollback `/tmp/d99_backup`). D100 (90/30 Hz) was the rejected first attempt. |
| **Behaviour-preserving DSP helper refactor (Jun 5, branch `feature/refactor-dsp-helpers`, D99)** | **+0.587 ns** (overall) / **+3.141 ns** (island) | 0 | Pure refactor, NO audible change: shared `onePoleShift` / `onePoleHighpass` / `peakFollower` helpers dedup 12 one-pole sites + 5 envelope followers; stale nested `hw/ip/clash/clash/` tree removed. **Proven equivalent to D98**: regenerated VHDL diff = renamed wires + source-comment lines + one harmless CSE only; `clash_lowpass_fir_types.vhdl` byte-identical. **Timing + utilisation IDENTICAL to D98** (island +3.141 / fabric +0.587 / WHS +0.018 / THS 0; DSP 135, BRAM 6, LUT 30740, FF 28830). bit/hwh md5 `83a64ffc6415fe2a3bc2aed47b6b19f9` / `8f7bb97945442d722c19ff44d0388904` (bit md5 changed ONLY because renamed wires alter the synthesis input). Deployed 5-site (board matched `83a64ffc`); programmatic mode-2 smoke PASS (~96.1 kHz frame cadence, clocks alive, mode control OK). Behaviourally == the bench-accepted D98; rollback D98 `18df313f` via `/tmp/d98_backup`. Latent finding: amp/RAT input-HP feedback pole is a no-op (`coef>>shift==0`); deferred fix. |
| **96 kHz conversion + whole-chain re-voicing, accepted on bench (Jun 5, branch `feature/96khz-conversion`, D98; restored temporarily at D107)** | **+0.587 ns** (overall) / **+3.141 ns** (island) | 0 | LATENCY_REDUCTION.md method 1: codec CS4344/CS5343 double-speed at 96 kHz (Pmod BCLK MCLK/4 -> MCLK/2; MCLK unchanged 12.288 MHz = 128fs; LRCK 96 kHz). **The DSP island clock is UNCHANGED (33 MHz, frequency-independent, 1 sample/cycle); at 96 kHz there are still ~347 island cycles per audio sample, far above the ~106-stage pipeline, so no timing change was needed.** The 4x oversampler interp + 15-tap decimation FIRs are ratio-based (fs-independent) -> Metal/RAT/Big Muff unchanged. Everything else fs-normalised was re-voiced to preserve corner Hz / centre Hz / time-constant ms: 7 biquads recomputed via RBJ at fs=96k Q14; shift one-poles +1; 11 onePoleU8 alphas via `1-sqrt(1-a)`; HP coeffs >>8->>9; envelope/LFO/smoothing time-constants halved; wah SVF f-byte map halved; reverb comb 1024->2048, diffusion 128->256, cab-mod 64->128 (+center/depth x2, LFO step 3->2); cab speaker FIR redesigned. **Whole design WNS +0.587 / 0 fail; island clk_fpga_1 WNS +3.141 / 0 fail (better than D97 +3.002); WHS +0.018; THS 0; phys_opt skipped (WNS>=0).** DSP `135` (D97 139 -- NO new multipliers, coefficient-only biquad edits), BRAM `6` (unchanged despite the doubled reverb), LUT `30740` (+~1100, doubled diffusion/cab-mod shift regs), FF `28830`. bit/hwh md5 `18df313f181bf972cb90b9dc2f21692a` / `8f7bb97945442d722c19ff44d0388904`. Deployed 5-site (board md5 matched at all four board resolution sites). **Bench (Pmod mode 2): codec locks at 96k double-speed, audio at correct pitch, all_off clean, re-voiced chain close to D97 voicing, no instability -- user-confirmed accepted ("合格"). Round-trip group delay ~halved.** **New accepted deployed baseline**, superseding D97 (`ad771d7c`, rollback `/tmp/d97_backup`). |
| **Oversampling item 2 / R5: 4x oversampled Big Muff clip cascade, accepted on bench (Jun 3, branch `feature/realism-bigmuff-oversample`, D90)** | **-0.036 ns** | -0.036 ns | Third oversampled clip. Big Muff's two cascaded soft clips (clip1 -> *208 -> clip2) run 4x then decimate (same os4x machinery; per-sub-sample nonlinearity is the soft-clip cascade `bigMuffOsCascade`, knees identical to the old clip1/clip2 so voicing preserved). **Cascade isolation is load-bearing:** cascade + FIR in one stage measured **-6.244 ns** (two muls + two clips in series); the deep cascade was moved to live ONLY in the history-update path (`bigMuffClipHistNext` -> `Vec 16` register, no FIR after), with the products stage reading all 15 FIR taps from the 16-deep history (no cascade) -- separating cascade-mul and FIR-mul into different register paths recovered it to **-0.036 ns / 1 fail** (worst path back to DS-1). Audio fabric (`clk_fpga_0`) **+0.434 ns / 0 fail**, WHS +0.051, THS 0. DSP `128` (+4 vs D89), BRAM `6`. -0.036 essentially meets timing (far above ~-0.7 bitcrusher boundary); the 3rd oversampler used most of the D89 40 MHz headroom (+1.846 -> -0.036) -- a 4th needs 33 MHz. bit/hwh md5 `93e8b220f94749b39c66e14ed2c431c6` / `13427a86e08dad9e7e39daf14547b05b`. Deployed 5-site (board md5 matched). **Bench (Pmod mode 2): all_off clean / no bitcrusher, Big Muff fizz reduced + voicing preserved, Metal/RAT/other + pitch unchanged -- user-confirmed accepted.** **New accepted deployed baseline**, superseding D89 (`1e9eb9ac`, rollback in git history). Metal+RAT+Big Muff all 4x oversampled. |
| **DSP island clock 50 -> 40 MHz (headroom) + RAT 4x oversampling, accepted on bench (Jun 3, branch `feature/realism-rat-oversample`, D89)** | **+1.846 ns** (island) / **+0.551 ns** (overall) | 0 | **FIRST fully-timing-clean build since D72 -- the whole design MEETS timing, 0 failing endpoints.** Adding a 2nd 4x oversampler (RAT + Metal) at 50 MHz blew DS-1 to -1.276 ns (routing congestion, DSP only +1). Fix: lower the DSP island clock FCLK_CLK1 50 -> 40 MHz (`island_integration.tcl`, `PCW_FPGA1_PERIPHERAL_FREQMHZ 50->40`, divisors `5/4->5/5` = 1000/5/5). The island is the ONLY FCLK_CLK1 consumer, 1 sample/cycle, frequency-independent (paceCount removed, D75), so 40 MHz still hugely exceeds the 48 kHz need while giving the DS-1 CARRY4 path a 25 ns (was 20 ns) budget -> DS-1 (negative since D72) is finally **+1.846 ns**. Pitch is set by the I2S/Pmod sample clock, NOT this island clock -> unaffected (bench-confirmed). Overall worst path is now a PS7 AXI clock (`clk_fpga_0`, +0.551 MET), not DS-1. Fabric stays 100 MHz (lowering the *fabric* remains forbidden -- I2S/Pmod CDC). Bundles RAT 4x oversampling + the `os4x*` shared-helper refactor (Metal+RAT). DSP `124`, BRAM `6`, WHS +0.024, THS 0. bit/hwh md5 `1e9eb9ac589e9647e699f6e2a16f27d5` / `ef17fe68e53af0ea8a8e6b6c77b859f7`. Deployed 5-site (board md5 matched). **Bench (Pmod mode 2): all_off clean, pitch correct, all effects healthy / no switch click (syncCtrl CDC works at 40 MHz), Metal + RAT alias fizz reduced -- user-confirmed accepted.** **New accepted deployed baseline**, superseding D88 (`d4c250be`, the 50 MHz-island rollback). Island now has +1.846 ns slack for more oversamplers/DSP; 33 MHz (1000/5/6) is the next step down. |
| **Oversampling item 2 / R5: 4x oversampled hard clip on Metal MT-2, accepted on bench (Jun 3, branch `feature/realism-metal-oversample2x`, D88)** | **-0.496 ns** | -18.903 ns | First oversampled nonlinearity. Metal hard clip at 4x then steep decimation (pushes >Nyquist harmonics out before they fold as inharmonic fizz; offline ~-12 dB vs 1x -- 2x only reaches ~-5.8 dB since >48 kHz still folds). Linear-interp upsample (shifts/adds, no mult; band-limited input makes it == a full anti-image FIR) -> 4 hard clips -> 15-tap symmetric anti-alias decimation FIR over the 192 kHz stream (Q9 sum=512, 8 folded mulS10). Clipped history = `Vec 12 Sample`. Pipeline-split (products: 8 folded products from ONE snapshot -> 3 Wide partials; mix: combine+satShift9) keeps the feedforward FIR near- but not-critical. Island (`clk_fpga_1`) **WNS -0.496 ns** / 81 fail -- worst path still DS-1 (`ds1_31_reg`, CARRY4=27), NOT the oversampler; audio fabric (`clk_fpga_0`) clean **+0.385 ns / 0 fail**, WHS +0.022, THS 0. DSP `123` (+1 vs D87), BRAM `6`. = D79's bench-clean -0.496 (well above the ~-0.7 bitcrusher boundary). bit/hwh md5 `d4c250be87400649b7b1ebf037fcf314` / `701067551c7399e6f96c888b7851cc59`. Deployed 5-site (board md5 matched). **Bench (Pmod mode 2): all_off clean / no bitcrusher, Metal alias fizz audibly reduced, voicing preserved, other pedals unchanged -- user-confirmed accepted.** **New accepted deployed baseline**, superseding D87 (`8a3754c1`, rollback in git history). Next: same 4x oversampler on RAT, then Big Muff (one model/phase); DS-1 excluded (critical path). |
| **Cab IR item 1 / R4 step A: 15-tap symmetric speaker-rolloff FIR, accepted on bench (Jun 3, branch `feature/realism-cab-speaker-fir`, D87)** | **-0.476 ns** | -19.164 ns | Additive 15-tap symmetric linear-phase FIR on the cab output (sharper >5 kHz rolloff, tames fizz, deepens model separation), per-model hand-designed coeffs (lowpass+presence, sum=256=unity DC); does NOT touch the accepted D71 nonlinear cab core. Symmetric -> 8 mulS10 (folded `(a+b)*c` on the DSP48 pre-adder). **Pipeline split is load-bearing:** a single-cycle 15-tap sum was too deep -- **WNS -1.106 ns** with the FIR itself the critical path (`cabSpk` 96x in the worst-100). A FIR is feedforward so it splits freely (unlike the D82 biquad feedback): products stage (8 products from ONE hist snapshot -> 3 Wide partial sums) + mix stage (combine+satShift8) moved it OFF the critical set (`cabSpk` 0x; DS-1 worst again), recovering to **-0.476 ns** / 65 fail. Audio fabric (`clk_fpga_0`) clean **+0.449 ns / 0 fail**, WHS +0.051, THS 0. DSP `122` (+16 vs D86's 106), BRAM `6`. In the accepted-clean band (≈ D82 -0.534 / D79 -0.496). bit/hwh md5 `8a3754c1f8cef9864c1b5e61eee289aa` / `a14e788ded059adf824ac70198c5cbff`. Deployed 5-site (board md5 matched). **Bench (Pmod mode 2): all_off clean / no bitcrusher, cab fizz reduced + model HF separation clearer, cab off + other effects unchanged -- user-confirmed accepted.** **New accepted deployed baseline**, superseding D86 (`1ab991c7`, rollback in git history). item 1 step A; step B (real 128-256-tap MAC convolution) is the next phase. |
| **Dynamic behaviour item 5b / R2 part 2: power-amp sag, accepted on bench (Jun 3, branch `feature/realism-amp-sag`, D86)** | **-0.397 ns** | -8.107 ns | Power-amp sag: a slow peak-follower of the master-input level (`ampSagEnvNext`; instant attack, slow release ~170 ms, reset 0 on bypass) lowers the master level on loud passages and recovers after. **DSP-free** -- reuses the existing `ampMasterFrame` `mulU8 (monoWet f) level` by dropping the level operand (`sagByte = min(env bits 22..17, level>>1)`, bounded to at most half the level = no choke); **JC-120 (idx 0) excluded** (solid-state). Island (`clk_fpga_1`) **WNS -0.397 ns** / 44 fail (all DS-1; `sag` 0x in the worst-100); audio fabric (`clk_fpga_0`) clean **+0.418 ns / 0 fail**, WHS +0.052, THS 0. DSP `106` (**unchanged** -- no new multiply), BRAM `6`. The -0.122 (D85) -> -0.397 swing is pure P&R run-to-run variance on the DS-1 path (DSP-free change), inside the accepted-clean band (≈ D83 -0.381). bit/hwh md5 `1ab991c7a406e8ec3ba72cdfa42eb347` / `3e47884cff493c6d06068ec91cd512c6`. Deployed 5-site (board md5 matched). **Bench (Pmod mode 2): all_off clean / no bitcrusher, tube amps sag+recover on loud chords with no pumping, JC-120 no sag, other effects unchanged -- user-confirmed accepted.** **New accepted deployed baseline**, superseding D85 (`b2d8a41b`, rollback in git history). item 5b (dynamic) complete. |
| **Dynamic behaviour item 5b / R2 part 1: Fuzz Face dynamic bias, accepted on bench (Jun 3, branch `feature/realism-fuzzface-bias`, D85)** | **-0.122 ns** | -0.160 ns | First dynamic / level-dependent change. A playing-level peak-follower envelope (`fuzzFaceBiasEnvNext`; instant attack, linear release ~43 ms, reset 0 on bypass) drifts the Fuzz Face clip knees (`biasShift = min(env>>4, 500k)`, `kneeP = 2.1M - biasShift`, `kneeN = 1.15M + biasShift>>1`) -- soft playing/rolled-back volume = cleaner asymmetric Ge knees, hard picking = knees pull together (harder / sputter). **No multiply -> no new DSP** (abs+shift+compare only); bit-exact bypass. Island (`clk_fpga_1`) **WNS -0.122 ns / only 3 fail, TNS -0.160** -- the **best WNS of the whole R3/R2 run**: the DSP-free change added zero DS-1 placement pressure and the P&R nearly closed the 50 MHz island. Audio fabric (`clk_fpga_0`) clean **+0.543 ns / 0 fail**, WHS +0.006, THS 0. DSP `106` (**unchanged** -- no multiply), BRAM `6`. bit/hwh md5 `b2d8a41b0f8389ca2ec851a2d267a7e6` / `e5efc8ca4241d99f3b2778ad9f7ba620`. Deployed 5-site (board md5 matched). **Bench (Pmod mode 2): all_off clean / no bitcrusher, Fuzz Face cleans up soft / sputters hard with no zipper, other pedals+amps unchanged -- user-confirmed accepted.** **New accepted deployed baseline**, superseding D84 (`dc030473`, rollback in git history). Lesson: envelope-modulated parameters (no new DSP) are timing-cheap on this island. |
| **Resonant tone stack item 3 / R3: AC30 chime + JCM800 mid into the shared amp biquad, accepted on bench (Jun 3, branch `feature/realism-amp-vox-marshall`, D84)** | **-0.472 ns** | -9.300 ns | Coefficient-only fill of the D83 shared amp tone-stack biquad mux: AC30 (idx 2) Vox chime upper-mid peak (f0=2200 Hz / Q=1.0 / +4 dB), JCM800 (idx 4) Marshall mid peak (f0=650 Hz / Q=0.8 / +4 dB); both hand-designed Q14 targets. Rockerverb (idx 3) + TriAmp (idx 5) stay flat = byte-identical. No new biquad/structure -- two more `ampScoopFeedforwardCoeffs`/`ampScoopFeedbackCoeffs` cases. Island (`clk_fpga_1`) **WNS -0.472 ns** / 49 fail (all DS-1; `scoop` 0x in the worst-100); audio fabric (`clk_fpga_0`) clean **+0.582 ns / 0 fail**, WHS +0.011, THS 0. DSP `106` (+1 vs D83 -- the larger/more-varied mux constants shifted Vivado's DSP inference), BRAM `6`. In the accepted-clean band (better than D79's bench-clean -0.496). bit/hwh md5 `dc030473688a456eae7239f6e5e55741` / `d981ffc3fec170d620ecdb50cc2ed7da`. Deployed 5-site (board md5 matched). **Bench (Pmod mode 2): all_off clean / no bitcrusher, AC30 chime + JCM800 mid audible, Fender/Rockerverb/TriAmp + other effects unchanged -- user-confirmed accepted.** **New accepted deployed baseline**, superseding D83 (`cef494cb`, rollback in git history). item 3 substantially complete. |
| **Resonant tone stack item 3 / R3: shared amp tone-stack biquad, Fender blackface mid scoop, accepted on bench (Jun 3, branch `feature/realism-amp-fender-scoop`, D83)** | **-0.381 ns** | -10.778 ns | Third R3 biquad, start of the amp-stack family work. ONE shared peaking biquad in the amp tone path (between `ampStage2Pipe` and `ampToneFilterPipe`, on `monoWet`), coefficients muxed by `ampModelIdxF`; reuses the D82 feedforward/recursive split. This phase fills only the Fender blackface mid scoop (JC-120 idx 0 + Twin idx 1, hand-designed f0=400 Hz / Q=0.7 / -5 dB, Q14 coeffs); models 2-5 use flat coeffs (b0=2^14, rest 0) = exact unity = byte-identical. Island (`clk_fpga_1`) **WNS -0.381 ns** / 52 fail (all DS-1; `scoop` 0x in the worst-100, biquad off the critical set via the split; worst path DS-1 `ARG__18__0`); audio fabric (`clk_fpga_0`) clean **+0.453 ns / 0 fail**, WHS +0.014, THS 0. DSP `105` (+5 over D82's 100), BRAM `6`, LUT 22082, FF 24811. **Better than D82 (-0.534) despite +5 DSP** -- the worry that a third biquad would bust the island budget did not hold (P&R + phys_opt landed favorably). bit/hwh md5 `cef494cb409e9a323b70659827d6c49c` / `82d2e14f21fb56b9bc62f247356a0d21`. Deployed 5-site (board md5 matched). **Bench (Pmod mode 2): all_off clean / no bitcrusher, Twin + JC-120 mid scoop audible, amps 2-5 + other effects unchanged -- user-confirmed accepted.** **New accepted deployed baseline**, superseding D82 (`ee295544`, rollback in git history). |
| **Resonant tone stack item 3 / R3: Big Muff ~700 Hz mid-scoop notch biquad (pipeline-split), accepted on bench (Jun 3, branch `feature/realism-bigmuff-notch`, D82)** | **-0.534 ns** | -17.195 ns | Second R3 biquad: a post-clip peaking biquad with negative gain (hand-designed f0=700 Hz / Q=0.8 / -10 dB dip, Q14 coeffs) carves the Big Muff mid scoop a one-pole LPF cannot make, between clip2 and the tone LPF. **Pipeline split is load-bearing:** the single-stage 5-multiply form measured **-0.659 ns** (74 fail, biquad feedback near-critical, `scoop` 62x in the worst-100, pressuring DS-1). An IIR feedback loop can't be naively pipelined, so the feedforward sum `b0*x+b1*x1+b2*x2` is precomputed a stage earlier into `fAcc3L` and the recursive stage closes the loop with only `-a1*y1-a2*y2` -- math identical, but the shorter feedback path moved the biquad **off the critical set** (`scoop` 0x in the worst-100; worst path now pure DS-1 `ARG__10__1`), recovering island WNS to **-0.534 ns** / 56 fail, TNS -17.195 (from -27.887). Audio fabric (`clk_fpga_0`) clean **+0.656 ns / 0 fail**, WHS +0.023, THS 0. DSP `100` (+5 over D81's 95), BRAM `6`, LUT ~21943, FF ~24645. The residual vs D81 (-0.193) is the +5 DSP placement pressure on the DS-1 path, not the biquad; -0.534 is only -0.038 ns from D79's bench-clean -0.496 (inside the accepted-clean band). bit/hwh md5 `ee295544e2e2caf22d5a3904aea045a1` / `e05afdb895ef7eda50f6204ac7d114eb`. Deployed 5-site (board md5 matched). **Bench (Pmod mode 2): all_off clean / no bitcrusher, Big Muff mid scoop audible, other pedals unchanged -- user-confirmed accepted.** **New accepted deployed baseline**, superseding D81 (`3a79745f`, rollback in git history). |
| **Resonant tone stack item 3 / R3: Tube Screamer ~720 Hz mid-hump biquad, accepted on bench (Jun 3, branch `feature/realism-tone-stack-biquad`, D81)** | **-0.193 ns** | -3.759 ns | First resonant biquad. A pre-clip `tubeScreamerMidFrame` peaking biquad (direct-form-I, hand-designed f0=720 Hz / Q=0.8 / +6 dB target, Q14 coeffs via new `mulS16`/`satShift14`) sits between the TS input HPF and drive multiply. The five multiplies are summed in **parallel** (the D79/Wah lesson) and `x1/x2/y1/y2` are pipeline-level state (cab-tap / RAT-prev idiom); bit-exact bypass when TS off; no GPIO/API/Frame change, only TS voicing. Island (`clk_fpga_1`) **WNS -0.193 ns**, 36 failing endpoints all intra-DS-1 CARRY4 (no biquad path in the top set); 100 MHz audio fabric (`clk_fpga_0`) clean at **+0.657 ns / 0 fail**; WHS `+0.036`, THS `0`. DSP `95` (+6 ≈ the 5 biquad multiplies vs D76's 89), BRAM `6`, LUT `22032`, FF `24594`. **Better than D79** (-0.496) and ≈ D78 (-0.173) -- the biquad added no meaningful timing cost (parallel multiplies + phys_opt). bit/hwh md5 `3a79745ffad8b72531a22587b5bcd3a1` / `a9b582634489170c16c2876388546aa4`. Deployed 5-site (board md5 matched). **Bench (Pmod mode 2 ADC->DSP->DAC): all_off bypass clean / no bitcrusher, TS mid hump audible, other pedals unchanged -- user-confirmed accepted.** **New accepted deployed baseline**, superseding D79 (`f0cb0276`, rollback in git history). |
| **Overdrive realism item 4 + 5a accepted on bench (Jun 1, branches `feature/realism-clip-hardness` / `feature/realism-klon-clean-blend`, D79)** | **-0.496 ns** | not recorded here | D79 keeps the D75 50 MHz DSP island, D76 XADC, and D78 footswitch + phys_opt stack, then changes only the dedicated Overdrive stage: fixed-shift per-model clip-hardness siblings selected by `odClipHardness`, and a model-5 CENTAUR/Klon clean-blend in `overdriveLevelFrame` using two parallel `mulU8` operations. No GPIO/API/topEntity/block-design change. Item 4 alone closed at `-0.173 ns` (= D78, no cost). The accepted item 5a bit has 32 failing endpoints, all intra-DS-1 CARRY4; the 100 MHz audio fabric is clean at **WNS +0.532 ns / 0 fail**. A one-multiply serial LERP rewrite was explicitly rejected at `-3.627 ns / 117 fail`; two parallel multiplies route better in this island. bit/hwh md5 `f0cb0276f27187d72476a2e773dd9a6e` / `5fa0b84e9fe852c68629c651f94e4a9d`. Deployed 5-site; user bench confirmed all_off clean / no bitcrusher. **Current accepted deployed baseline.** |
| **3PDT footswitch input + phys_opt audio fix accepted on bench (Jun 1, branch `feature/footswitch-preset-fxtoggle`, D78)** | **-0.173 ns** | not recorded here | D78 adds `axi_footswitch_input @ 0x43D50000` (M21) via `footswitch_integration.tcl`, reading three latching 3PDT footswitches on RP pins 11/12/35. Clash DSP voicing is byte-identical to D76. Adding the AXI master perturbed P&R enough to produce a D74-class bitcrusher before phys_opt (`199d25ea` RP and `e610dc58` PMOD-JA rejected); enabling post-place + post-route `phys_opt_design` (`AggressiveExplore`) recovered the 50 MHz island slack to `-0.173 ns`, with 100 MHz audio fabric **+0.683 ns / 0 fail**, WHS `+0.014`. bit/hwh md5 `45e78763...` / `aa12a661...`. Bench audio clean and final RP-pin footswitch function accepted. Superseded as deployed baseline by D79, but phys_opt remains load-bearing. |
| **FP02M XADC re-add on the D75 island, accepted on bench (May 31, branch `feature/fp02m-xadc-island-readd`, D76)** | **-0.368 ns** | -2.779 ns | D76 re-enables the XADC Wizard (`xadc_wiz_a0 @ 0x43D40000`, VAUX1 = A0 = E17/D18) on top of the D75 50 MHz DSP island by un-commenting the two `create_project.tcl` lines (`add_files xadc_a0.xdc`, `source xadc_integration.tcl`); the Clash DSP is untouched (voicing byte-identical to D73/D75) and the island (`cc_dsp_in`/`cc_dsp_out`, FCLK0=100/FCLK1=50, `set_clock_groups`) is preserved. The D74 XADC rejection (bitcrusher on the ADC path) did NOT recur: the cause was the D74 -11 ns / 100 MHz audio-AXIS P&R degradation, not the XADC. Here the 100 MHz `clk_fpga_0` fabric (the whole audio AXIS datapath) closes with **WNS +0.614 ns, 0 failing endpoints** -- the only 22 failing endpoints are intra-`clk_fpga_1` (50 MHz DSP island, the DS-1 distortion arithmetic, the same place D75 had slack). Overall routed WNS `-0.368 ns` (better than D75 `-0.706 ns`), WHS `+0.045 ns`, THS `0`. LUT `21286`-class, FF `~23968`, BRAM `6`, DSP `89`. bit/hwh md5 `9fdecae0c7d7cf3c59422cec2b30368f` / `a9fd74082482aa1b074fc3c31ccd6283`. **Bench (Pmod mode 2 ADC->DSP->DAC): all_off bypass clean (NO bitcrusher), FP02M A0 sweep span ~2999, pedal -> Wah POSITION audible sweep follows, no self-oscillation after the Q-cap (byte 80).** New deploy baseline. |
| **DSP clock-domain island: clash @ 50 MHz (FCLK_CLK1), 100 MHz fabric, accepted on bench (May 31, branch `feature/dsp-multicycle`, D75)** | **-0.706 ns** | -15.826 ns | D75 runs ONLY `clash_lowpass_fir_0` at FCLK_CLK1 = 50 MHz; the fabric (AXI / DMA / `i2s_to_stream` / Pmod / HDMI) stays at FCLK_CLK0 = 100 MHz so the I2S/Pmod CDCs are byte-identical to D72 (the rejected global-50 MHz build buzzed because lowering the whole fabric broke those CDCs). `island_integration.tcl` (additive, `block_design.tcl` NOT edited) enables FCLK1, adds `rst_island_50M`, and inserts two `axis_clock_converter` (`cc_dsp_in` 100->50, `cc_dsp_out` 50->100) around the DSP AXIS. Two required supports: `paceCount` removed in `Pipeline.hs` (`acceptReady = readyOut`, the only frequency-dependent term in the AXIS handshake), and `syncCtrl` (2-FF + 2-cycle stability) CDC on the 12 control words in `LowPassFir.hs` (without it every effect/knob switch clicks, because 100 MHz GPIO words latch a transient mixed value on the 50 MHz side). `audio_lab.xdc` `set_clock_groups -asynchronous` over all 7 domains (clk_fpga_0 / clk_fpga_1 / clk 48 MHz Pmod / bclk / mclk 24 / audio_ext 12.288 / pixel 40) removes the `rst_ps7_0_100M -> pmod_master` inter-clock reset that was the -4.2 ns worst path (harmless `CRITICAL WARNING 12-4739`: BD clocks undefined at synth elaboration, applied at impl -- confirmed by the worst path moving inter->intra). Routed WNS `-0.706 ns` (worst now an intra-`clk_fpga_0` AXI-Lite GPIO write, harmless and always present under the DSP path), WHS `+0.052 ns`, THS `0`; failing endpoints `53 / 64479`. LUT `21286`, FF `23968`, BRAM `6`. bit/hwh md5 `4a0b3dae1e56574ad596dbd6a3f0c98f` / `347d3e553a8ca96733ee27e904a1d25f`. Stage-split (DS-1 boost/clip) and Frame-width reduction (1067->731 bits) were tried first and did NOT improve WNS (route-bound). XADC dropped (D74 bitcrusher); the Clash DSP voicing is unchanged from D73. **Bench (Pmod mode 2 ADC->DSP->DAC): all_off bypass clean, no switch click, pitch correct, all effects + GUI + HDMI healthy -- 完璧.** Full record in `DSP_ISLAND_CLOCK_DESIGN.md`. D73 / D72 are the 100 MHz rollback baselines. |
| **Wah Cry Baby retune + position API split + volume curve fix accepted on bench (May 29, branch `feature/wah-cry-baby-d73`, D73)** | **-10.910 ns** | -11052.431 ns | D73 ships three refinements on top of D72: (1) Cry Baby GCB-95 sweep retune via `basePositionToFByte` anchor changes from `12 / 20 / 33 / 53 / 80` to `15 / 24 / 37 / 53 / 73` (~450..2200 Hz at fs = 48 kHz); (2) volume curve fix in `wahVolumeFactor` -- new two-segment piecewise linear `Unsigned 10` factor with byte 128 (UI 50 %) = factor 256 = unity, byte 255 (UI 100 %) = factor 510 = +6 dB. New `mulU10` helper in `AudioLab.FixedPoint` re-uses the existing DSP slice; (3) Python position API split into `wah_position_byte` (percent 0..100) + `wah_position_raw_byte` (raw 0..255). No new GPIO, no `block_design.tcl` edit, no new stage in `Pipeline.hs`. Routed WHS `+0.022 ns`, THS `0.000 ns`; failing endpoints `3397 / 61312`. Utilization: LUT `20920` (D72 `21023`, delta `-103`), FF `22672` (D72 `22691`, delta `-19`), BRAM `6` (unchanged), DSPs `89` (**unchanged** -- the mulU10 widening did re-use an existing DSP slice as planned). WNS delta vs D72 (-10.387) is `-0.523 ns` -- inside acceptance band. Worst path stays inside the existing DS-1 distortion section (`ds1_7_reg[784]/C -> ARG__2__2__0_i_1_psdsp/D`); no Wah state register chain appears in the top-100. bit/hwh md5 `d1343291184d8e3465f735bef8856d38` / `aad985fe57b0ff263efc8fc5e09c3c3e`. Deploy 5-site PASS (md5-matched); `AudioLabOverlay(download=True)` PASS; `set_wah_settings` percent + raw round-trip PASS (`position=50 -> byte 128`, `position_raw=200 -> byte 200` with percent cache preserved, both-paths set raises `ValueError`); `scripts/diagnose_pmod_loopback.py` PASS (no QUANT!/STAIR!, CLIP_COUNT 0). **External-instrument bench audition PASS:** focused high end, vocal-like peak with high Q, warm-but-not-muddy heel, bright-but-not-painful toe; UI VOLUME=50 audibly unity; UI VOLUME=100 audibly +6 dB without breakup; BIAS shifts the sweep range as expected; no pop / click on Wah OFF -> ON transitions. **D73 is the new accepted deployed baseline;** D72 (`eacc4f35...` / `eaa88898...`, WNS `-10.387 ns`) is the immediate rollback baseline. |
| **Wah effect on dedicated AXI GPIO (May 28, deploy pending board reachability, branch `feature/wah-effect`, D72)** | **-10.387 ns** | -12177.222 ns | D72 adds a new resonant band-pass Wah on `axi_gpio_wah @ 0x43D30000`. A new `hw/Pynq-Z2/wah_integration.tcl` is sourced from `create_project.tcl` after `pmod_i2s2_integration.tcl`; `block_design.tcl` itself is NOT edited. The integration bumps `ps7_0_axi_periph/NUM_MI` from 19 to 20 and wires the GPIO to a new `wah_control` topEntity port. `hw/ip/clash/src/AudioLab/Effects/Wah.hs` implements a Chamberlin parallel-update state-variable filter with five pipeline-level state registers (`wahPosSmooth`, `wahFByteR`, `wahQBandR`, `wahLow`, `wahBand`). **First Vivado pass regressed WNS from D71.2 `-9.413 ns` to `-18.966 ns`** (delta `-9.55 ns`); worst path was `wahPosSmooth_reg[6]/C -> wahBand_reg[4]/D` with 34 logic levels including 3 DSP48E1 in series (positionToFByte mul + q*band mul + high*fByte mul fused into one combinational block). Wah.hs was refactored to pre-register `positionToFByte` and the `q * oldBand` product into `wahFByteR` / `wahQBandR` so each subsequent state update consumes at most one DSP + small adders. After the refactor: routed WNS `-10.387 ns`, TNS `-12177.222 ns`, WHS `+0.052 ns`, THS `0.000 ns`; failing endpoints `3877 / 61321`. WNS delta vs D71.2 is `-0.974 ns` -- within historically accepted range (D70 was `-1.302 ns`, accepted). The new top-100 worst paths are dominated by the existing DS-1 distortion section (`ds1_7_reg[154]/C -> ARG__4__0__0_i_4_psdsp/D`); no Wah state register chain appears in the top-100 paths, confirming the pipeline split worked. Utilization: LUT `21023` (+1067 vs D71.2 `19956`), FF `22691` (+431 vs `22260`), BRAM `6` (unchanged), DSPs `89` (+6 vs `83` -- the new Wah multiplies: positionToFByte's `base*bias`, `q*oldBand`, low's `f*oldBand`, band's `high*fByte`, volume's `band*factor`, plus one auxiliary). bit/hwh md5 `eacc4f35bd81c3afcdbb808baa4c8d47` / `eaa888985c319841147d1ce73d6601b5`. **Deploy + bench deferred**: PYNQ-Z2 at 192.168.1.9 unreachable (ping / ssh timeout) at deploy time. D71.2 (`9a739f90...` / `f28f0867...`, WNS `-9.413 ns`) is the rollback baseline until the deploy + bench cycle completes. Acceptance gate: all_off bypass D69-clean, Wah OFF bit-exact bypass, Wah ON sweep audibly moves the BPF centre frequency, Q audibly changes peak width, VOLUME 50 unity-ish without breakup, BIAS audibly shifts the sweep range, `scripts/diagnose_pmod_loopback.py` PASS with no QUANT!/STAIR! and CLIP_COUNT 0. |
| **Cabinet multi-band pseudo-IR speaker character candidate (May 27, deployed for bench, D71)** | **-9.413 ns** | -10233.182 ns | D71 extends D70 with multi-band pseudo-IR blend in `AudioLab/Effects/Cab.hs`: (1) FIR coefficients redesigned with sums 256/260/264 and stronger Nyquist rejection (model 0: -16, model 1: -24, model 2: -44 at air 0); (2) presence/cone breakup via `softClipK(cabPresenceKnee)` on early component (open 25%, combo 12.5%, closed 12.5%); (3) HF fizz suppression via `input - mainSat` residual subtraction (open 12.5%, combo 25%, closed 50%); (4) per-model mid-body emphasis (open 0%, combo 6.25%, closed 12.5%); (5) speaker compression knees widened to 5.6M/4.0M/2.8M; (6) body resonance knees retuned to 2.4M/1.6M/1.2M. One `softClipK` added (LUT-only, no DSP48). WNS delta 0.000 ns vs D70 (incremental build reused D70 placement). D71.1 retune (fizz/presence/body fraction grid-search) and D71.2 (20 kHz non-monotonic fix + 8 kHz rolloff deepening) applied on top. Routed WHS `+0.051 ns`, THS `0.000 ns`; design summary failing endpoints `3219 / 60414`. Slice LUTs `19956`, Slice Registers `22260`, BRAM `6`, DSPs `83`. bit/hwh md5 `9a739f904aef0955b7e59837a2c33d41` / `f28f08674d25c65a48cd240ae31a578a` deployed (after D71.2). Deploy checks PASS: board md5 matches local, `AudioLabOverlay(download=True)`, ADC HPF True. `scripts/diagnose_pmod_loopback.py` PASS with no `QUANT!` / `STAIR!` and CLIP_COUNT 0. External-instrument bench is pending; D70 is the rollback baseline until D71 is auditioned. |
| **Cabinet speaker character improvement candidate (May 26, deployed for bench, D70)** | **-9.413 ns** | -10233.182 ns | D70 improves cabinet simulator in `AudioLab/Effects/Cab.hs`: FIR coefficient table redesign (model 0 direct:body 2.9:1, model 2 0.46:1; Nyquist rejection improved from 28/20/-20 to -8/-2/-32), saturated body-resonance term in `fAcc3L` via `satShift8 -> softClipK -> resize << N` (no new DSP48), per-model `softClipK` speaker knee in `cabLevelMixFrame` (open 5.2M / combo 4.2M / closed 3.4M). Secondary fix: D54 BRAM patch applied to `trueDualPortBlockRamWrapper_0.vhdl` (shared variable -> signal + `ram_style "distributed"`). WNS delta -1.302 ns vs D69; includes both Cab combinational-logic addition and i2s_to_stream BRAM wrapper P&R change. Routed WHS `+0.051 ns`, THS `0.000 ns`; design summary failing endpoints `3219 / 60414`. Slice LUTs `19956`, Slice Registers `22260`, BRAM `6`, DSPs `83`. bit/hwh md5 `aab907a4e56260543dc48adb35a3f09f` / `f28f08674d25c65a48cd240ae31a578a` deployed. Deploy checks PASS: board md5 matches local, `AudioLabOverlay(download=True)`, ADC HPF True. `scripts/diagnose_pmod_loopback.py` PASS with no `QUANT!` / `STAIR!` and CLIP_COUNT 0. External-instrument bench is pending; D69 remains the rollback baseline until D70 is auditioned. |
| **Amp Drive Mode saturation candidate (May 26, deployed for bench, D69)** | **-8.111 ns** | -9246.014 ns | D69 strengthens only Amp Sim Drive Mode in `AudioLab/Effects/Amp.hs` by retuning existing Drive-mode constants. `ampPreLpfDriveDarken` is `6 / 8 / 12 / 20 / 20 / 30`, `ampSecondStageDriveBonus` is `22 / 30 / 42 / 62 / 74 / 88`, `ampDrivePosDelta` is `16_200 / 85_800 / 232_400 / 374_400 / 462_000 / 615_000`, and `ampDriveNegDelta` is `13_500 / 74_100 / 199_200 / 322_400 / 407_000 / 541_200` for JC-120 / Twin Reverb / AC30 / Rockerverb / JCM800 / TriAmp Mk3. The Drive deltas remain per-model fixed scalars (`Unsigned 3 -> Signed 25`) evaluated from the current `ampCharForModel` values; no runtime multiplier, model-index change, GPIO change, helper addition, register, IIR, DSP operation, `Pipeline.hs`, `LowPassFir.hs`, GUI, HDMI, Pmod RTL, or `block_design.tcl` edit was introduced. Routed WHS `+0.052 ns`, THS `0.000 ns`; design summary failing endpoints `3157 / 60278`. Slice LUTs `19717`, Slice Registers `22156`, BRAM `6`, DSPs `83`. bit/hwh md5 `6a1834b7f66693f82663c2c8a2fda28b` / `927191b506c68588eaae286f4ccce112` deployed 5-site for bench. Deploy checks PASS: board md5 matches local, `AudioLabOverlay(download=True)`, ADC HPF True, R19 `0x23`, VERSION `0x00480001`, MODE 3 mute. `scripts/diagnose_pmod_loopback.py` PASS with no `QUANT!` / `STAIR!`; DMA-sine Amp-state smoke at 1 kHz / `-12 dBFS` had `clip_d=0` for all_off, Amp OFF, JC-120 Clean/Drive, AC30 Drive, Rockerverb Drive, JCM800 Drive, and TriAmp Drive. External-instrument bench is pending; D68 remains the accepted rollback baseline until D69 is auditioned. |
| **Global Amp / Distortion / Overdrive real-pedal constants retune (May 25, deployed, branch `feature/global-amp-dist-od-real-pedal-retune-20260525-192457`, D68)** | **-7.333 ns** | -9235.637 ns | D68 bulk-retunes existing constants only in `Amp.hs`, `Distortion.hs`, and `Overdrive.hs` after explicit user risk acceptance and with rollback to D67 recorded. No model index, GPIO mapping, AXI address, helper topology, pipeline shape, `Pipeline.hs`, `LowPassFir.hs`, GUI, HDMI, Pmod RTL, or `block_design.tcl` changed; no register, IIR, helper, cascade, or DSP operation was added. Routed WHS `+0.051 ns`, THS `0.000 ns`; design summary failing endpoints `3595 / 60350`, worst endpoint `compLevelPipe_reg[638]/C -> compGain_reg[7]/D`. Slice LUTs `19842`, Slice Registers `22246`, BRAM `6`, DSPs `83`. bit/hwh md5 `cabb9bca3fbcc41f06f8b9fe8301cff1` / `299485480dcc46aa0c679cef8f1a048a` deployed 5-site. Deploy checks PASS: board md5 matches local, `AudioLabOverlay(download=True)`, ADC HPF True, R19 `0x23`, VERSION `0x00480001`, MODE 3 mute, CLIP_COUNT 0. `scripts/diagnose_pmod_loopback.py` PASS with no `QUANT!` / `STAIR!`; user-reported external bench PASS for all_off bypass and all Amp / Distortion / Overdrive models. D68 is accepted and becomes the deployed baseline. |
| **JCM800 amp model constants-only retune (May 25, deployed, branch `feature/jcm800-amp-model-retune`, D67)** | **-8.204 ns** | -9300.746 ns | D67 changes only the JCM800 (`amp_model_idx == 4`) entries in `Amp.hs`: `ampPreLpfDriveDarken` `16 -> 13`, `ampSecondStageDriveBonus` `48 -> 54`, and `ampDriveNegDelta` `231_000 -> 200_000`. `ampDrivePosDelta` stays `264_000`; JCM800 `ampTrebleGain` / `presenceTrim` stay unchanged. All other amp model entries are byte-exact; no `Distortion.hs` / `Overdrive.hs` / `Compressor.hs` / `Pipeline.hs` / `LowPassFir.hs`, no GPIO / GUI / HDMI / Pmod / block-design edit, no helper add / swap / cascade, no new IIR / register / `mulU8` / `mulU12`. Routed WNS `-8.204 ns` is a `-0.188 ns` slip vs D66 `-8.016 ns`, still inside the current deployed band; WHS `+0.034 ns`, THS `0 ns`; design summary failing endpoints `3284 / 60261`, primary `clk_fpga_0` setup group `2229 / 52745`, worst endpoint `ARG__17/CLK -> ds1_5_reg[1032]/D`. Slice LUTs `19836`, Slice Registers `22174`, BRAM `6`, DSPs `83`. bit/hwh md5 `70b5dc7d972510c26fbb3b1014aa06eb` / `dc42290dc7fb46d7486068cc1d11032a` deployed 5-site. Deploy-time checks PASS: board md5 matches local, `AudioLabOverlay(download=True)`, ADC HPF True, MODE 3 mute. `scripts/diagnose_pmod_loopback.py` PASS with no QUANT! / STAIR! flags and clean CLIP_COUNT. Bench: all_off bypass clean by user report; JCM800 Drive was auditioned after correcting the Pmod MODE offset and amp GPIO address; user requested Adopt & merge. D67 is accepted and becomes the deployed baseline. |
| **DS-1-only asymSoftClip knee retune (May 24, deployed, branch `feature/ds1-only-asymsoftclip-retune`, D66)** | **-8.016 ns** | -9648.033 ns | D66 changes only the two DS-1 knee constants in `Distortion.hs` `ds1ClipFrame`: `kneeP` `2_400_000 -> 1_900_000`, `kneeN` `2_000_000 -> 1_900_000`. Existing `asymSoftClip` topology preserved; no helper added / swapped / cascaded, no `asymHardClip`, no `Pipeline.hs`, no `LowPassFir.hs`, no GPIO / GUI / HDMI / Pmod / block-design edit. ACTUAL `asymSoftClip` invocation count remains `3`, `asymHardClip` remains `0`, `mulU8` / `mulU12` counts remain `13` / `8`. Routed WNS `-8.016 ns` is better than D62's `-8.497 ns`; WHS `+0.051 ns`, THS `0 ns`; design summary failing endpoints `3236 / 60251`, primary `clk_fpga_0` setup group `2242` failing endpoints, worst endpoint `ARG__7__1/CLK -> ds1_5_reg[1032]/D`. Slice LUTs `19712`, Slice Registers `22160`, BRAM `6`, DSPs `83`. bit/hwh md5 `52f0e9937993dca11272d561f6cf6b32` / `d75d38394a529ac3524e0a64f73bcd34` deployed 5-site. Deploy-time checks PASS: board md5 matches local, `AudioLabOverlay(download=True)`, ADC HPF True, VERSION `0x00480001`, MODE 3 mute, CLIP_COUNT 0. `scripts/diagnose_pmod_loopback.py` PASS with no QUANT! / STAIR! flags. User requested merge to `main`; D66 was accepted and was the deployed baseline until D67. |
| **Rejected distortion-wide asymSoftClip knee-only retune (May 24, deploy + rollback, branch `feature/retune-distortion-asymsoftclip-constants`, D64)** | **-7.903 ns** | -5457.133 ns | D64 applied the strictest D62 / D63 cumulative rule (constants only on existing `asymSoftClip` invocations, no helper added / swapped / cascaded, no `Pipeline.hs` edit). Five numeric edits in `Distortion.hs`: TS9 `kneeN` 2_500_000 -> 2_700_000 (gap 400k -> 200k, more symmetric to match real D1/D2 antiparallel); DS-1 `kneeP` 2_400_000 -> 2_200_000, `kneeN` 2_000_000 -> 2_100_000 (gap 400k -> 100k, nearly symmetric to match real op-amp diode pair); Fuzz Face `kneeP` 1_900_000 -> 2_000_000, `kneeN` 1_400_000 -> 1_200_000 (gap 500k -> 800k, more asymmetric per ElectroSmash). ACTUAL `asymSoftClip` invocation count unchanged (3); ACTUAL `asymHardClip` invocation count unchanged (0); no DSP / BRAM / register edit. Routed WNS `-7.903 ns` (**+0.594 ns vs D62** -- notably better, not worse), WHS `+0.052 ns`, THS `0 ns`, failing endpoints `2038 / 52739`. Slice LUTs `19690` (-10 vs D62), Slice Registers `22304` (+24 vs D62), BRAM `6` (unchanged), DSPs `83` (unchanged). bit/hwh md5 `ea647168adda426d4d7d35656c7ca91f` / `a15147c3c5f832826f78c588c3a7551b` deployed 5-site. Deploy-time programmatic smoke PASS (Pmod mode 2 safe-clean 3 s: FRAME_COUNT delta `144150`, CLIP_COUNT delta `0`, ADC HPF True, MUTE 3). **Bench audition (CLAUDE.md spec connection, no Pmod direct loopback): per-pedal directions all validated (TS9 smoother / Fuzz Face more broken-up as intended, DS-1 in right direction but needs more magnitude), BUT all_off bypass had MORE noise than D62 -- the same P&R-induced bypass-path regression class as D58 / D59 / D60 / D61 v2 / D63. D64 rejected on this basis.** **Revised load-bearing rule**: D62 (3 constants in one Overdrive model) is now understood to have been *load-bearing minimal scope*, not "constants-only is safe in general". Touching 5 constants across 3 distortion pedals in one build IS structural in the Vivado-P&R sense. Future retakes must do ONE pedal at a time, smallest possible numeric move per knee. PYNQ rolled back to D62 bit/hwh `349ebbe6...` / `3a90e966...`; post-rollback safe-clean smoke PASS (FRAME_COUNT delta `144151`, CLIP_COUNT `0`, ADC HPF True, MUTE 3). Source / Clash artifacts / bit / hwh reverted to D62; `docs/ai_context/DISTORTION_ASYMSOFTCLIP_RETUNE_RESEARCH.md` survives on main as the per-pedal research deliverable. Do not redeploy this bit. |
| **Rejected DS-1 Distortion fidelity attempt (May 24, deploy + rollback, branch `feature/improve-ds1-distortion-model`, D63)** | **-8.426 ns** | -6452.238 ns | D63 added BD-2-style coefficient retunes to `Distortion.hs` `ds1*Frame` functions (no `Pipeline.hs` change): (a) `ds1ClipFrame` chained `asymSoftClip` (Q2 emulator, softP=`3_000_000` / softN=`2_600_000`) before `asymHardClip` (op-amp diode pair emulator, symmetric hardP=hardN=`2_200_000`), (b) `ds1MulFrame` drive coefficient `8 -> 10` (~1x..~11x), (c) `ds1ToneFrame` alpha base `96 -> 80` (range 80..207). Both clip helpers are pure compare-add-shift; **no new DSP48E1 (count stays at 83), no new `Pipeline.hs` register**. Routed WNS `-8.426 ns` (+0.071 ns vs D62), TNS `-6452.238 ns`, WHS `+0.051 ns`, THS `0 ns`, failing endpoints `2127 / 52725`. Slice LUTs `19755` (+55 vs D62's `19700`), Slice Registers `22195` (-85 vs D62's `22280`), BRAM `6` (unchanged), DSPs `83` (unchanged). bit/hwh md5 `b9bb64260d0c9b2ed86f9543a8392359` / `6fb1210f60970118d80993035460342d` deployed 5-site. Deploy-time programmatic smoke PASS (Pmod mode 2 safe-clean 3 s: FRAME_COUNT delta `144154`, CLIP_COUNT delta `0`, ADC HPF True, MUTE 3). **Bench audition (CLAUDE.md spec connection, no Pmod direct loopback) FAILED on all four criteria simultaneously**: (1) bypass all_off produced a *bit-crusher-like* artifact (a different failure mode from D58/D59/D60/D61 v2's HF saturation noise; suggests AXIS-stream sample quantisation / glitching), (2) DS-1 D20/D50/D80 sweep did NOT produce the intended progression, (3) DS-1 T30/T50/T70 sweep was indistinguishable (sound too anomalous to discriminate), (4) RAT / TS9 / BD-2 sounded different from D62 -- the DS-1-only source edit LEAKED into other pedals and the entire Overdrive section. **Load-bearing lesson**: even when DSP/BRAM/register count and WNS all look fine, **adding a second clip-helper invocation inside a single existing stage is a structural change** in the Vivado-P&R sense -- the cascade increases combinational depth and fan-out, and Vivado's downstream placement / routing of unrelated nets shifts enough to leak an audible artifact onto safe-bypass and onto unrelated effect paths. PYNQ rolled back to D62 bit/hwh `349ebbe6...` / `3a90e966...`; post-rollback safe-clean smoke PASS (FRAME_COUNT delta `144153`, CLIP_COUNT `0`, ADC HPF True, MUTE 3). Source / Clash artifacts / bit / hwh reverted to D62 in the rollback commit; `docs/ai_context/DS1_MODEL_RESEARCH.md` survives on main as the lasting deliverable. Do not redeploy this bit. Next DS-1 retake (D63.1 or later) must use *at most one* clip helper per stage, no cascade. |
| **BD-2 Overdrive coefficient-only retune (May 24, deployed, branch `feature/bd2-coeff-d62`, D62)** | **-8.497 ns** | -5876.740 ns | D62 is the accepted retake of the BD-2 fidelity goal after the D61 v1 / v2 bench rejections. **Three numeric edits in `Overdrive.hs` at `model == 2`**: `odDriveK 2` 6 -> 7 (matches OCD's drive ceiling per the documented two-cascaded ~40 dB op-amp character), `odKneeP 2` 3_000_000 -> 2_400_000 (early-onset soft clip per the PedalPCB / Chuck D. Bones breadboard finding that BD-2 has audible breakup well below mid-drive, not "transparent" as the prior value implied), `odKneeN 2` 2_700_000 -> 1_900_000 (P/N gap widened from 300k to 500k for the strongest even-harmonic asymmetry in the six-model lineup). `odSafetyKnee 2` unchanged at 3_400_000. `Pipeline.hs` byte-for-byte unchanged; no new register stage, no new `mulU8` / `mulU12` invocation, no new combinational fan-out; per the D61 rule. Five non-BD-2 models (TS9 / OD-1 / Jan Ray / OCD / Centaur) byte-exact preserved. Routed WNS `-8.497 ns` is essentially identical to D58.2 `-8.495 ns` (delta `-0.002 ns`, noise floor), TNS `-5876.740 ns` (improved over D58.2's `-9052.753`), WHS `+0.053 ns`, THS `0 ns`, failing setup endpoints `2107 / 52730` (better than D58.2's `3224 / 60227`). Utilization after place: Slice LUTs `19700` (`-13` vs D58.2's `19713`), Slice Registers `22280` (`+170` vs D58.2's `22110`), Block RAM Tile `6` (unchanged), DSPs `83` (unchanged from D58.2). bit/hwh md5 `349ebbe609ac15f58d8b676d2dedee94` / `3a90e966c5d76762b60ba3ab0e982685` deployed 5-site to PYNQ-Z2 192.168.1.9. Deploy-time programmatic smoke PASS (Pmod mode 2 safe-clean 3 s: FRAME_COUNT delta `144150`, CLIP_COUNT delta `0`, ADC HPF True, MUTE 3). **Bench audition (CLAUDE.md spec connection, external source -> Pmod IN, Pmod OUT -> monitor, no direct loopback) PASS on all three criteria: bypass all_off as quiet as D58.2 (D58/D59/D60/D61 v2 class of HF saturation noise did NOT reappear -- pure-constant changes do not trigger the artifact), BD-2 G20 / G50 / G80 audibly earlier-clip + asym vs D58.2 BD-2, TS9 / OD-1 / Centaur identical to D58.2.** D62 is the new deployed baseline. The near-zero WNS delta vs D58.2 is the load-bearing signal that Vivado P&R landed on essentially the same placement, which D58 / D59 / D60 / D61 v2 collectively proved is the prerequisite for keeping the safe-bypass path clean. |
| **Rejected BD-2 Overdrive model fidelity v2 (May 24 build, deploy + rollback, branch `feature/improve-bd2-overdrive-model`, D61 v2)** | **-8.083 ns** | -5959.880 ns | D61 v2 added BD-2-only behaviour to `Overdrive.hs` model index 2: (a) pre-clip ~1 kHz leaky-LPF whose HP residual drives an upper-mid emphasis shift (`bd2PreLpShift = 3`, `bd2PreEmphShift = 2`), (b) a first-stage mild `asymSoftClip` in the boost stage with `bd2FirstKneeP = 3_400_000` / `bd2FirstKneeN = 3_100_000` (softer than the existing main-clip knees so it engages only on hot picking), and (c) a post-clip ~5 kHz leaky-LPF fizz-guard (`bd2PostLpShift = 1`) that feeds the tone IIR. All three new IIRs were expressed as `y = prev + ((x - prev) >> N)` (pure shift + add + sub, no `mulU8`), so DSP count stays at `83` (same as D58.2). Pipeline.hs adds two BD-2-only Sample feedback registers (`bd2PreLpPrev`, `bd2PostLpPrev`) that reset to 0 when overdrive_model != 2 or OD is off. Other five Overdrive models (TS9 / OD-1 / Jan Ray / OCD / Centaur) byte-exact unchanged. Routed WNS `-8.083 ns` (+0.412 ns vs D58.2), WHS `+0.052 ns`, THS `0 ns`, failing endpoints `2172 / 52784`. Utilization: Slice LUTs `20048` (+335 vs D58.2's `19713`), Slice Registers `22277` (+167 vs D58.2's `22110`), BRAM `6` (unchanged), DSPs `83` (unchanged). bit/hwh md5 `065a869a34e2bde86051c6a96c4aaa2f` / `927a3dfcc9819171226cb0686348fb01` deployed 5-site. Deploy-time programmatic smoke PASS (FRAME_COUNT delta ~480k per 10 s, CLIP_COUNT delta 0 every case, GUI healthy, MUTE 3). **Bench audition with CLAUDE.md spec connection (no Pmod direct loopback) confirmed BD-2 G20 / G50 / G80 = edge-of-breakup -> canonical BD-2 -> fuzzy/splatty, BD-2 tone 30 / 50 / 70 = dark / flat / bright with no ice-pick, TS9 / OD-1 / Centaur unchanged from D58.2 (no leak to other models). However safe-bypass (every `effect_on = False`, Pmod mode 2 ADC -> DSP -> DAC) was clearly noisier than D58.2 on the same A/B by ear -- the D58 / D59 / D60 class of bypass-path artifact, not flagged by CLIP_COUNT / FRAME_COUNT / timing summary. D61 v2 rejected on this basis.** PYNQ rolled back to D58.2 bit/hwh `1c9071b5...` / `21c1ca7a...`; post-rollback safe-clean smoke PASS (FRAME_COUNT delta 144150, CLIP_COUNT 0, ADC HPF True, MUTE 3). Source / Clash artifacts / bit / hwh reverted to D58.2 in the rollback commit; `docs/ai_context/BD2_MODEL_RESEARCH.md` survives on main as the lasting deliverable. Do not redeploy this bit. |
| **Rejected BD-2 Overdrive model fidelity v1 (May 24 build, not deployed -- DSP count out of budget, branch `feature/improve-bd2-overdrive-model`, D61 v1)** | **-7.891 ns** | -6724.798 ns | D61 v1 used `onePoleU8` (two `mulU8` per IIR pole) for the BD-2 pre and post one-pole IIRs, plus one `mulU8` for the upper-mid emphasis. **DSP count climbed from `83` to `88` (+5)** which is the same class of DSP-count delta that triggered the D58 bypass regression (DSP 83 -> 87). v1 was rejected without bench-listen on this basis; the shift-only retake landed as D61 v2 above. Routed WNS `-7.891 ns` (+0.604 ns vs D58.2), TNS `-6724.798 ns`, WHS `+0.051 ns`, THS `0 ns`. Utilization: Slice LUTs `20084` (+371 vs D58.2), Slice Registers `22269` (+159 vs D58.2), BRAM `6` (unchanged), DSPs `88` (`+5` vs D58.2 -- the load-bearing reject reason). bit/hwh md5 `13429faf72b87015725dfee2ee814dee` / `fe6cd05ef0ea78f4fe5c15abf4bc9432` recorded for traceability; not deployed and must not be redeployed. |
| **Rejected Compressor target-gain split, control-only retake (May 23 build, May 24 deploy + rollback, branch `wns-compressor-gain-pipeline`, D60)** | **-8.300 ns** | -8836.632 ns | D60 kept the Compressor gain-calculation split but avoided D59's full-`Frame` target pipeline. `compTargetStage1` registered only the Compressor control word, active flag, clamped excess, and ratio byte; `compTargetStage2` performed `excessU12 * ratioByte`, reduction shift/clamp, and target-gain calculation; `compGainNext` did the existing smoothing/register update from the registered target. The audio frame stayed on the original `compLevelPipe -> compApplyPipe` data path, so only Compressor gain reaction was delayed by a few samples; global DSP order and audio-frame latency were unchanged. Compressor coefficients, threshold/ratio/response/makeup semantics, effect order, GPIOs, `topEntity` ports, GUI, Pmod I2S2, `Amp.hs`, DS-1 / Distortion, `block_design.tcl`, and Vivado strategy were unchanged. Timing: WNS improved by `+0.195 ns` vs D58.2 (`-8.495 ns -> -8.300 ns`), hold clean (`WHS +0.043 ns`, `THS 0.000 ns`). Utilization after place: Slice LUTs `19728` (`37.08 %`), Slice Registers `22253` (`20.91 %`), Block RAM Tile `6` (`4.29 %`), DSPs `83` (`37.73 %`). Routed top setup path stayed DS-1-side `ARG__6__3` DSP48E1 -> `ds1_5_reg[1015]` (slack `-8.300 ns`), intentionally untouched. bit/hwh md5 `078f39c78991f1b36e6bfd1806b830a5` / `48160ae4acdf3abb9d1abf14dd65cc6d`. **Deployed 2026-05-24 and audio-rejected: cold-power-cycled PYNQ + explicit `AudioLabOverlay(download=True)` forced a fresh PL program, deploy-time programmatic smoke PASSed (`FRAME_COUNT delta = 144148..144154`, `CLIP_COUNT delta = 0` across `all_off` / `comp_on_mild` / `comp_on_stronger` / `comp_off_again`, GUI keep + dsp 20 s holds clean, LCD compact-v2 OK, final mute=3), but on-bench audio verification by ear FAILED with high-frequency saturation noise audible even in safe-bypass (all `effect_on = False`).** Same class of regression as D58 / D59 -- Vivado P&R-induced bypass-path artifact that the macroscopic timing summary and CLIP_COUNT do not flag. Rolled back to D58.2 (bit/hwh `1c9071b5...` / `21c1ca7a...`); post-rollback Pmod mode 2 safe-clean smoke PASS (`FRAME_COUNT delta = 144153`, `CLIP_COUNT delta = 0`, `ADC HPF True`, `VERSION 0x00480001`). Do not redeploy this bit. D60 source (`Compressor.hs`, `Pipeline.hs`, regenerated Clash artifacts under `hw/ip/clash/vhdl/LowPassFir/`) is reverted to D58.2 in the rollback commit so the tree matches the deployed bit; this row + `DECISIONS.md` D60 + `CURRENT_STATE.md` "Superseded D60 attempt note" are the discoverable history. |
| **Rejected Compressor target-gain split with full frame carry (May 23, deployed then rolled back, branch `wns-compressor-gain-pipeline`, D59)** | **-8.138 ns** | -8756.266 ns | D59 split the same Compressor gain-calculation path but carried the full `Frame` through `compTargetStage1Pipe`, `compTargetPipe`, and `compGainFramePipe`, adding 254 registers. Timing improved by `+0.357 ns` vs D58.2 and programmatic smoke was clean (`FRAME_COUNT delta = 144150`, `CLIP_COUNT delta = 0`), but the user reported the same high-frequency saturation-noise pattern previously fixed by D58.1/D58.2. Treat D59 as audio-rejected despite its timing win; do not redeploy bit/hwh `a42358803798acc1e63ef5d4abd45b33` / `1ddd377d077401ccf60a9096d319ed52`. The PYNQ-Z2 was restored to D58.2 bit/hwh md5 `1c9071b5f2e1eec63ef6abbcfcacbf02` / `21c1ca7a6ddd5c26fd39f8746abe28d8`, and rollback smoke passed (`ADC HPF True`, Pmod mode 2 readback `2`, `FRAME_COUNT delta = 144154`, `CLIP_COUNT delta = 0`). |
| **Balanced Amp Drive Mode (fixed-scalar retake) (May 23, branch `feature/amp-drive-mode-balanced-gain-v2`, D58.2)** | **-8.495 ns** | -9052.753 ns | D58.2 picks the same audible Drive-strengthening target as D58 (`feature/amp-drive-mode-balanced-gain`, commit `797467c`) but re-shapes the knee deltas as **per-model fixed scalars** (`Unsigned 3 -> Signed 25`, D55 signature; no `ch` argument forwarded from `ampAsymClip`) sized to approximate D58's first-stage `ch * factor` evaluated at each model's own `ampCharForModel` peak (`JC-120 13_000 / Twin 58_000 / AC30 130_000 / Rockerverb 210_000 / JCM800 264_000 / TriAmp Mk3 336_000` for pos; `11_000 / 50_000 / 113_000 / 180_000 / 231_000 / 300_000` for neg). `ampPreLpfDriveDarken` `5..24` and `ampSecondStageDriveBonus` `14..56` are bit-for-bit the same as D58 (no DSP cost in either). The D58 attempt was rolled back because its `ch * factor` deltas added four DSP48E1 multiplications (`83 -> 87`), and the resulting Vivado P&R shift introduced an audible high-frequency saturation noise on the ADC -> DAC bypass path even with Amp OFF and full safe bypass (a sub-path the macroscopic timing summary did not flag). D58.2's load-bearing metric is **DSP `83 / 220 (37.73 %)` -- same as D55 and four below D58's `87`**, which is the proof that the fixed-scalar form actually removed the multiplications that triggered the P&R regression. WNS regresses by `0.264 ns` vs D55 (`-8.231 ns -> -8.495 ns`), inside the historical `-7..-9 ns` deploy band and well above the `-9.5 ns` hard gate. WHS `+0.051 ns`, THS `0.000 ns` (hold clean). 3224 / 60227 failing setup endpoints (5.35 %). Utilization after place: Slice LUTs `19713` (`37.05 %`; -73 vs D55's `19787`), Slice Registers `22110` (`20.78 %`; -50 vs D55's `22160`), Block RAM Tile `6` (`4.29 %`; unchanged), DSPs `83` (`37.73 %`; same as D55). bit/hwh produced at `hw/Pynq-Z2/bitstreams/audio_lab.bit` / `audio_lab.hwh` (sha `93f31348...` / `25991dc0...`), deployed 5-site to PYNQ-Z2 192.168.1.9 via `scripts/deploy_to_pynq.sh`. Programmatic smoke PASS: `ADC HPF True`; six-model ctrlD readback OK across Clean + Drive (12 cases); Pmod I2S2 MODE writes `0 / 1 / 2 / 3` all readback OK via `scripts/pmod_i2s2_mode.py`; **safe bypass (every `effect_on = False`) + mode-2 DSP 3 s `CLIP_COUNT delta = 0` (FRAME_COUNT `+144150` = exact 48 kHz cadence) -- the D58 regression guard**; Amp OFF (others default) 3 s CLIP_COUNT delta = `0`; TriAmp Mk3 + Drive (full chain) 3 s CLIP_COUNT delta = `0`; `scripts/run_encoder_hdmi_gui.py --live-apply --skip-rat --pmod-mode dsp` starts cleanly with `live=ON apply=OK` and no Python exceptions in a 12 s hold. **On-bench audio verification by ear ("D58 の高音域飽和ノイズが消えた", "Drive で D55 より歪む", "D57 より穏やか", per-model voicing differences) pending the user's session.** `DECISIONS.md` D58.2. |
| **Six researched amp voicings (May 22, branch `feature/replace-amp-models-six-pack-researched`, D55)** | **-8.231 ns** | -9667.459 ns | D55 retires the legacy 4-model D52 lineup (jc_clean / clean_combo / british_crunch / high_gain_stack) and replaces it with six inspired-by voicings (JC-120 / Twin Reverb / AC30 / Rockerverb / JCM800 / TriAmp Mk3). `axi_gpio_amp_tone.ctrlD[2:0]` is widened from 2-bit to 3-bit; `ctrlD[6:3]` reserved; `ctrlD[7] = ampDriveMode` (D54 layout preserved). `Amp.hs` carries per-model voicing tables (`ampModelDarken`, `ampPreLpfDriveDarken`, `ampSecondStageDriveBonus`, `ampDrivePosDelta`, `ampDriveNegDelta`, `ampTrebleGain` 6-entry, `presenceTrim` 6-entry). `ampAsymClip` now takes a 3-arg signature (`Unsigned 3 -> Unsigned 8 -> Bool -> Sample -> Sample`); the model idx selects a per-model Drive-mode knee delta. No new register stage, no new GPIO, no new `topEntity` port, no `block_design.tcl` change. WNS regresses 0.143 ns vs the D54 baseline (`-8.088 ns -> -8.231 ns`), still well inside the historical `-7..-9 ns` deploy band and well above the `-9.5 ns` hard gate. WHS `+0.053 ns`, THS `0.000 ns` (hold clean). 3189 failing setup endpoints out of 60262 (5.3 %). Utilization after place: Slice LUTs `19787` (`37.19 %`; +0.27 % vs D54's `19644`), Slice Registers `22160` (`20.83 %`; -0.03 % vs D54), Block RAM Tile `6` (`4.29 %`; unchanged), DSPs `83` (`37.73 %`; -1.82 % vs D54's `87` -- the per-model knee delta math reuses the same multipliers as the legacy character-byte arithmetic, no new multiplier instantiated). bit/hwh produced at `hw/Pynq-Z2/bitstreams/audio_lab.bit` / `audio_lab.hwh`. Subsequently deployed to PYNQ-Z2 192.168.1.9 (sha `8df39b06...` / `9fb470c7...`). The D56 (`4ceb55c`) and D57 (`8d9d4d1`) follow-ups raised the Drive-mode coefficients further and were audibly over-driven on the bench, so both were rolled back and the D55 bit was re-deployed. The D55 Drive coefficients are now superseded by the **D58.2 Balanced Drive retake** above; D55 remains the structural baseline (six-model lineup, 3-bit ctrlD field, per-model voicing tables, real-DSP-branch Clean/Drive) and the D55 bit is the canonical rollback target if D58.2's Drive needs to be undone (`git show 314b7c6:hw/Pynq-Z2/bitstreams/audio_lab.bit` recovers it without a Vivado rebuild). `DECISIONS.md` D55 / D58.2. |
| **Amp Sim Clean/Drive DSP branch (May 21, deployed, branch `feature/amp-clean-drive-dsp-mode`, D54)** | **-8.088 ns** | -9282.231 ns | D54 retires the D53 in-band character-byte shift and adds a real Clash DSP branch on `amp_drive_mode`. `axi_gpio_amp_tone.ctrlD` is now a two-field bit-pack: `ctrlD[7] = ampDriveMode` (0=Clean, 1=Drive), `ctrlD[6:2] = 0` reserved, `ctrlD[1:0] = ampModelIdx`. The Clash `Amp.hs` decodes the two fields via `ampModelIdxF` / `ampDriveModeF` and adds real Drive-mode branches: `ampAsymClip intensity drive x` shrinks knees further (linear in the per-model character byte) and drops the negative-side post-knee shift from `>> 3` to `>> 2`; `ampPreLowpassFrame` subtracts 12 from alpha; `ampSecondStageMultiplyFrame` adds `+24` to the Q7 second-stage gain. The legacy `ampModelSel` byte quantiser is removed. The Clash-emitted `ADAU1761_topEntity_trueDualPortBlockRamWrapper` (4-entry shared-variable BRAM that Vivado 2019.1 refused to infer from-scratch with the original template) was hand-patched to a signal-based array with `ram_style = "distributed"`, restoring the from-scratch build path without changing the functional contract; this drops Block RAM Tile usage from D50's 9 to 6 (the 4-entry RAM now lives in LUT-distributed RAM instead of two BRAM18E1 tiles). No new GPIO, no address change, no `block_design.tcl` change. WNS regresses by `0.103 ns` vs the D50 deployed baseline (`-7.985 ns -> -8.088 ns`), still inside the historical `-7..-9 ns` deploy band and well above the `-9.5 ns` hard gate. WHS `+0.051 ns`, THS `0 ns` (hold remains clean). Utilization after place: Slice LUTs `19644` (`36.92%`), Slice Registers `22196` (`20.86%`), Block RAM Tile `6` (`4.29%`), DSPs `87` (`39.55%`). Deployed to PYNQ-Z2 via `PYNQ_HOST=192.168.1.9 bash scripts/deploy_to_pynq.sh`. Bench A/B (Clean vs Drive audibly distinct under the same amp model, CLIP_COUNT stays bounded across Drive mode) pending. `DECISIONS.md` D54. |
| Pre-distortion-refactor (May 1) | -7.722 ns | -4613.495 ns | Original baseline. Audio works in practice. |
| Distortion `model_select` attempt (May 4) | -15.067 ns | -7308.247 ns | 8-way model mux; **rejected**, never deployed. |
| Pedal-mask refactor (May 4) | -7.801 ns | -7381.742 ns | Seven independent pedal stages. Deployed; live-verified. Setup slack roughly baseline-equivalent. |
| **Noise-suppressor refactor (May 5, deployed)** | **-7.111 ns** | -7683.480 ns | Adds `axi_gpio_noise_suppressor` (`0x43CC0000`) and the `nsLevelPipe -> nsEnv -> nsGain -> nsPipe` block in place of the legacy hard gate. WNS improves by 0.690 ns vs the pedal-mask baseline; the new block has one fewer feedback register (no `gateOpen` boolean stage) so it is slightly cheaper than what it replaced. Hold remains clean (`WHS = +0.053 ns`, `THS = 0.000 ns`). |
| **Compressor add (May 5, deployed)** | **-7.516 ns** | -8815.426 ns | Adds `axi_gpio_compressor` (`0x43CD0000`) and the `compLevelPipe -> compEnv -> compGain -> compApplyPipe -> compMakeupPipe` block between the noise suppressor and the overdrive. Same shape as the noise suppressor (one envelope-input register stage, two feedback registers, one apply stage) plus a separate makeup multiply stage so each register holds a single multiply. WNS regresses by 0.405 ns vs the noise-suppressor build (`-7.111 ns -> -7.516 ns`), still inside the historical -7..-9 ns deploy band. Hold remains clean (`WHS = +0.052 ns`, `THS = 0.000 ns`). |
| **Real-pedal voicing pass (May 6, deployed)** | **-6.405 ns** | -8806.714 ns | Constants and clip-helper choice retuned inside the existing register stages of `LowPassFir.hs` (Overdrive / clean_boost / tube_screamer / RAT / metal / Compressor / Noise Suppressor / Cab IR / Reverb / EQ); see `REAL_PEDAL_VOICING_TARGETS.md` and `DECISIONS.md` D16. No new register stage, no new GPIO, no `block_design.tcl` change, no `topEntity` port change. WNS **improves** by 1.111 ns vs the Compressor build (`-7.516 ns -> -6.405 ns`); the swapped `asymSoftClip` / `softClipK` / hysteresis logic happens to route slightly better than the symmetric `softClip` / hard-knee paths it replaced. Hold remains clean (`WHS = +0.052 ns`, `THS = 0.000 ns`). |
| **Reserved-pedal implementation (May 7, deployed)** | **-7.535 ns** | -11297.604 ns | Adds three independent register-staged pedal sections (`ds1` 5 stages, `big_muff` 5 stages, `fuzz_face` 4 stages) to `fxPipeline` after `metalLevelPipe`; `distortionPedalsPipe = fuzzFaceLevelPipe`. No new GPIO, no new `topEntity` port, no `block_design.tcl` change. The three new stage chains add 14 register stages worth of pipeline depth but each stage holds at most one multiply / one one-pole IIR / one clip helper (same shape as the existing `clean_boost` / `tube_screamer` / `metal`). WNS regresses by 1.130 ns vs the voicing-pass build (`-6.405 ns -> -7.535 ns`); still inside the historical -7..-9 ns deploy band. Hold remains clean (`WHS = +0.051 ns`, `THS = 0.000 ns`). TNS rises (more failing endpoints because the new stages share the same critical-clock domain) but no single endpoint is dramatically worse. |
| **Amp Simulator named models (May 7, deployed)** | **-8.122 ns** | -13519.447 ns | Adds the `ampModelSel :: Unsigned 8 -> Unsigned 2` quantiser and a per-band darken (`0/2/8/16`) on `ampPreLowpassFrame`'s alpha. No new register stage, no new GPIO, no `topEntity` port change, no `Frame` field change. WNS improves 0.609 ns vs the audio-analysis build (`-8.731 ns -> -8.122 ns`); the cheaper alpha computation happens to route slightly better than the previous unbiased path. Hold stays clean (`WHS = +0.051 ns`, `THS = 0.000 ns`). |
| **Amp/Cab real-voicing pass (May 7, deployed)** | **-7.917 ns** | -13100.457 ns | Retunes only existing Amp/Cab stages: amp HPF/gain/clip/presence/resonance/master constants and the 4-tap `cabCoeff` model table. No new GPIO, no new `topEntity` port, no `block_design.tcl` change, no new register stage. WNS regresses by 0.382 ns vs the reserved-pedal build (`-7.535 ns -> -7.917 ns`), still inside the historical -7..-9 ns deploy band and far from the rejected -15 ns mux failure. Hold remains clean (`WHS = +0.051 ns`, `THS = 0.000 ns`). |
| **Audio-analysis voicing fixes (May 7, deployed)** | **-8.731 ns** | -13665.555 ns | Retunes only existing Compressor / Overdrive / Amp / Cab stages from the recording analysis in `AUDIO_RECORDING_ANALYSIS.md`: compressor threshold/knee/response constants, Overdrive drive/clip/safety constants, Amp treble/presence/LPF/safety constants, and the 4-tap `cabCoeff` table. No new GPIO, no new `topEntity` port, no `block_design.tcl` change, no new register stage. A trial Cab post-mix `softClipK 3_400_000` build reached WNS = -9.891 ns and was **not** deployed; the final deployed build keeps `cabLevelMixFrame` on the existing `softClip`. WNS regresses by 0.814 ns vs the Amp/Cab build (`-7.917 ns -> -8.731 ns`), still inside the accepted deploy band. Hold remains clean (`WHS = +0.051 ns`, `THS = 0.000 ns`). |
| **Amp Simulator fizz-control pass (May 8, deployed)** | **-8.022 ns** | -13937.512 ns | Retunes only existing Amp Simulator stages to reduce high-frequency fizz: `ampPreLowpassFrame` model darken becomes `0/4/12/24`, `ampTrebleGain` becomes character-aware with a small model trim, `ampResPresenceProductsFrame` adds model-dependent presence trim, and `ampPowerFrame` / `ampResPresenceMixFrame` safety knees move from `3_500_000` to `3_400_000`. No new GPIO, no new `topEntity` port, no `block_design.tcl` change, no new register stage, no Delay implementation. WNS improves by 0.709 ns vs the audio-analysis build (`-8.731 ns -> -8.022 ns`); hold remains clean (`WHS = +0.052 ns`, `THS = 0.000 ns`). Utilization after place: Slice LUTs 21809 (40.99%), Slice Registers 18675 (17.55%), Block RAM Tile 7 (5.00%), DSPs 158 (71.82%). |
| **LowPassFir behavior-preserving split (May 8, deployed)** | **-8.022 ns** | -13937.512 ns | Splits `LowPassFir.hs` into `AudioLab.Types`, `FixedPoint`, `Control`, `Axis`, `Effects.*`, and `Pipeline` modules without changing DSP behavior, coefficients, bit widths, `Frame` shape, pipeline order, `topEntity` ports, `block_design.tcl`, GPIOs, Python API, Notebook UI, or Chain Presets. VHDL/IP and bit/hwh rebuilt successfully; WNS delta is 0.000 ns vs the deployed Amp fizz-control baseline and hold remains clean (`WHS = +0.052 ns`, `THS = 0.000 ns`). Utilization after place is unchanged: Slice LUTs 21809 (40.99%), Slice Registers 18675 (17.55%), Block RAM Tile 7 (5.00%), DSPs 158 (71.82%). Deployed to PYNQ-Z2 with `PYNQ_HOST=192.168.1.9`; smoke test confirmed ADC HPF, absence of `axi_gpio_delay_line`, legacy `axi_gpio_delay`, amp models, and chain presets. |
| **Internal mono DSP pipeline (May 9, deployed)** | **-8.155 ns** | -6492.876 ns | Keeps external AXI/I2S as 48-bit stereo but runs the active DSP path from ADC Left as a mono source, discards Right input, and duplicates the mono result back to output Left/Right. `topEntity`, `block_design.tcl`, GPIOs, Python API, Notebook UI, and Chain Presets are unchanged. `Frame.fLast` carries TLAST from input to output; `fxPipeline` paces accepted DMA frames so the fixed-latency DSP core keeps one output per accepted input during short S2MM backpressure. WNS regresses by 0.133 ns vs the minimal mono / `37ef4c7` baseline (`-8.022 ns -> -8.155 ns`), still inside the deployed -6..-9 ns band. Hold remains clean (`WHS = +0.052 ns`, `THS = 0.000 ns`). Utilization after place: Slice LUTs 15473 (29.08%), Slice Registers 14914 (14.02%), Block RAM Tile 7 (5.00%), DSPs 83 (37.73%). Deployed to PYNQ-Z2 and DMA Case A/B/C completed without timeout; output L/R matched exactly after skip 16 and Right input rejection was confirmed. |
| **HDMI GUI Phase 4 integrated framebuffer (May 15, deployed)** | **-8.163 ns** | -6599.061 ns | Adds the HDMI GUI framebuffer path around the existing AudioLab DSP: `axi_vdma_hdmi` (`0x43CE0000`), `v_tc_hdmi` (`0x43CF0000`), `v_axi4s_vid_out_hdmi`, Digilent `rgb2dvi_hdmi`, `clk_wiz_hdmi`, `rst_video_0`, and `axi_smc_hdmi`. The HDMI path is sourced from `hw/Pynq-Z2/hdmi_integration.tcl`; Clash/DSP source, `topEntity`, existing GPIO names/addresses, and `axi_gpio_delay` legacy RAT semantics are unchanged. GUI RGB888 is packed as DDR `GBR888` and scanned out as a 24-bit stream. WNS regresses by only 0.008 ns vs the internal mono baseline (`-8.155 ns -> -8.163 ns`) and remains inside the Phase 4 deploy gate. Hold remains clean (`WHS = +0.051 ns`, `THS = 0.000 ns`). Utilization after place: Slice LUTs 18619 (35.00%), Slice Registers 20846 (19.59%), Block RAM Tile 9 (6.43%), DSPs 83 (37.73%). Deployed to PYNQ-Z2; smoke confirmed ADC HPF/R19, legacy GPIO contract, chain presets, HDMI IP in HWH/ip_dict, and static GUI frame VDMA scanout with no VDMA error bits. Phase 5A/5C later confirmed visible output on the 5-inch LCD and adopted the top-left 800x480 framebuffer viewport. |
| **HDMI GUI Phase 4C resource profile (May 15, docs/script only)** | **-8.163 ns** | -6599.061 ns | No Vivado rebuild and no bit/hwh regeneration. Reused the Phase 4 routed timing/utilization reports and profiled the deployed framebuffer path on PYNQ-Z2. PL before/after remains the Phase 4 delta vs internal mono: WNS `-8.155 ns -> -8.163 ns`, TNS `-6492.876 ns -> -6599.061 ns`, WHS `+0.052 ns -> +0.051 ns`, THS `0.000 ns -> 0.000 ns`, LUT `15473 -> 18619` (`+3146`), Registers `14914 -> 20846` (`+5932`), BRAM `7 -> 9` (`+2`), DSP `83 -> 83` (`+0`). Runtime profile showed cold render `2.979 s`, change-driven render avg `0.276 s`, full-frame copy avg `0.206 s`, VDMA/VTC start `0.0023 s`, and 60-second hold process CPU avg/max `0.352%` / `0.418%`. Phase 5A/5C later confirmed the 5-inch LCD's practical visible area is framebuffer `x=0,y=0,w=800,h=480`; native 800x480 timing remains a deferred experiment, not a timing baseline. |
| **Phase 6H native 800x480 attempt (May 16, NOT deployed)** | **-8.138 ns** | -6405.865 ns | Switched the HDMI path in `hw/Pynq-Z2/hdmi_integration.tcl` to a native `800x480` signal at pixel clock `40.000 MHz`, `H total 1056`, `V total 628`, `rgb2dvi kClkRange=3` (VCO `800 MHz`, lower edge of the valid band). Vivado built without errors and timing stayed inside the historical -7..-9 ns deploy band (WHS `+0.040 ns`, THS `0.000 ns`), but on the actual 5-inch LCD the receiver did not lock onto the 800x480 hybrid timing and the screen rendered fully white. The Phase 6H bit/hwh were therefore **rejected** and **not committed**; archived under `/tmp/fpga_guitar_effecter_backup/phase6h_failed_white_screen/`. See `docs/ai_context/history/hdmi_phases/HDMI_GUI_PHASE6H_NATIVE_800X480_TIMING.md`. |
| **HDMI GUI Phase 6I C2 SVGA 800x600 (May 16, deployed, commit `5332b7e`)** | **-8.096 ns** | -6389.430 ns | Moves the HDMI output from `1280x720 / 74.250 MHz` to standard **VESA SVGA `800x600 @ 60 Hz`, pixel clock `40.000 MHz`**, `H total 1056` (`fp 40 / sync 128 / bp 88`), `V total 628` (`fp 1 / sync 4 / bp 23`), `rgb2dvi kClkRange=3`. Same H/V totals as the rejected Phase 6H, but the active area is now SVGA-standard `800x600` (the 800x480 compact-v2 GUI is composed at framebuffer `(0,0)` and rows `480..599` stay black); the LCD's HDMI receiver recognises the standard DMT mode and the on-screen UI is no longer right-shifted. `hw/Pynq-Z2/hdmi_integration.tcl` now sets `CONFIG.VIDEO_MODE {Custom}` in a first `set_property` pass before the per-field `GEN_*` values (otherwise the v_tc 6.1 IP silently keeps its `1280x720p` preset and ignores the overrides), sets `GEN_F0_VBLANK_HSTART = GEN_F0_VBLANK_HEND = HDMI_ACTIVE_W (800)` explicitly, and drops `GEN_CHROMA_PARITY` (does not exist on v_tc 6.1). `audio_lab_pynq/hdmi_backend.py` defaults to `DEFAULT_WIDTH=800`, `DEFAULT_HEIGHT=600`; VDMA programmed to `HSIZE=2400`, `STRIDE=2400`, `VSIZE=600`. Compared to the deployed HDMI Phase 4 baseline (`-8.163 ns / -6599.061 ns`): WNS improves by `0.067 ns` and TNS by `210 ns`. WHS `+0.040 ns`, THS `0.000 ns` (hold remains clean). Utilization after place: Slice LUTs `18618` (`35.00%`), Slice Registers `20846` (`19.59%`); essentially unchanged from Phase 4. PYNQ-Z2 smoke confirmed `v_tc_hdmi GEN_ACTSZ = 0x02580320` (V=600 / H=800), ADC HPF `True`, R19 `0x23`, no VDMA error bits; the 5-inch LCD shows the compact-v2 Pip-Boy UI left-aligned. **Runtime gotcha:** the `40 MHz × M=20 = 800 MHz` PLL VCO sits at the absolute lower edge of `rgb2dvi v1.4 kClkRange=3`'s valid band, so a second `Overlay(..., download=True)` in the same session can knock the PLL out and drop the LCD to white even with VDMA/VTC still healthy; recovery is a PYNQ-Z2 power cycle. `audio_lab_pynq/notebooks/HdmiGuiShow.ipynb` works around this at the application layer by reading `GEN_ACTSZ` first and attaching with `download=False` when the bit is already loaded. See `docs/ai_context/history/hdmi_phases/HDMI_GUI_PHASE6I_800X480_TIMING_SWEEP.md` and `DECISIONS.md` D25. |
| **Rotary encoder input IP for HDMI GUI (May 17, local build, not deployed)** | **-8.395 ns** | -6609.224 ns | Adds the separate PL module-reference AXI-Lite input IP `axi_encoder_input` / `enc_in_0` at `0x43D10000..0x43D1FFFF`, connected on `ps7_0_axi_periph` M17 after the HDMI VDMA M15 (`0x43CE0000`) and VTC M16 (`0x43CF0000`). The IP decodes three rotary encoders in PL (CLK / DT / SW on Raspberry Pi header pins `F19/V10/V8`, `W10/B20/W8`, `V6/Y6/B19`) without consuming PMOD JA/JB pins; later D48/D49/D50 assign PMOD JB to the active Pmod I2S2 audio path and add `pmod_status_0` on M18. HDMI remains VESA SVGA `800x600 @ 40 MHz`; compact-v2 remains top-left 800x480 with the lower 120 rows black. Clash/DSP, `topEntity`, existing effect GPIO addresses, and `GPIO_CONTROL_MAP.md` are unchanged. Final routed timing from build3 (`/tmp/fpga_guitar_effecter_backup/phase7f7g_vivado_build3.log`): WHS `+0.052 ns`, THS `0.000 ns`. WNS regresses by `0.299 ns` vs the Phase 6I C2 deployed baseline (`-8.096 ns`) and remains inside the historical `-7..-9 ns` band. Utilization after place: Slice LUTs `19095` (`35.89%`), Slice Registers `21259` (`19.98%`), Block RAM Tile `9` (`6.43%`), DSPs `83` (`37.73%`). Local bit/hwh were generated, but PYNQ deploy and physical encoder smoke were deferred because the three encoders are not wired yet; do not claim standalone encoder operation until `scripts/test_encoder_input.py` and HDMI GUI live control pass on hardware (`DECISIONS.md` D35). |
| **Pmod I2S2 mode 2 RIGHT-to-LEFT mirror (May 20, deployed, branch `feature/pmod-i2s2-dsp-clean-fix`, D50)** | **-7.985 ns** | -8737.608 ns | D50 adds a 32-bit `mode2_right_snapshot` register inside `pmod_i2s2_master.v` that captures `dsp_dac_sdin_i` on every `bclk_fall_pre` during the RIGHT slot (`bit_idx[5] == 1`) indexed by `slot_idx`. In mode 2 (`cfg_mode == 2'd2`) the DAC SDIN mux replays `mode2_right_snapshot[bit_idx[4:0]]` regardless of LEFT or RIGHT slot. Works around two `i2s_to_stream` IP bugs that produced "slight but audible" distortion in D49 mode 2 even with every effect off: (1) the IP's `i2sIn` LEFT extraction mismatches the Pmod-master deserializer (DMA captures of `axis_li_tdata` showed LEFT spiking near `-0 dBFS` and a steady DC offset while RIGHT matched Pmod-master exactly; bit-prevalence in LEFT had bits 16..21 set ~half as often as RIGHT, isolating the bug to the IP's LEFT slot extraction), (2) the IP's `i2sOut` updates `so` on BCLK rising edges with no setup margin to the next BCLK rising edge, so the DAC can latch the OLD bit and play a 1-BCLK-shifted bitstream. The mirror snaps the IP's `so` value one MCLK cycle after each BCLK rising (`bclk_fall_pre` = `mclk_phase == 1`, well after the IP's combinational settle), then replays it in both slots of the next frame. The user hears mode-2 mono = chain RIGHT output in both ears with a one-frame (~21 us) delay. Mode 0 / 1 / 3 paths in `pmod_i2s2_master.v` unchanged; `block_design.tcl`, `pmod_i2s2_integration.tcl`, `GPIO_CONTROL_MAP`, `LowPassFir.hs`, `topEntity`, HDMI / encoder integration, compact-v2 GUI, notebooks all untouched. WNS **improves** by `0.536 ns` vs the D49 deployed baseline (`-8.521 ns -> -7.985 ns`); the mirror is a single 32-bit register so it adds essentially no critical-path logic and apparently lets P&R route a touch better. Hold remains clean (`WHS = +0.050 ns`, `THS = 0.000 ns`). PYNQ-Z2 smoke (branch `feature/pmod-i2s2-dsp-clean-fix`): `--mode loopback --confirm-loopback` mode 1 still clean (frame_count rising at 48 kHz, CLIP_COUNT=0, peak_abs ~ -6 dBFS). `diagnose_pmod_i2s2_dsp_clean.py --duration 15` mode 2 clean reports MODE=2, frame delta `+720,720 / 15 s`, CLIP_COUNT=0, peak_abs ~ -13 dBFS. `--ab-overdrive` Phase A clean → Phase B Overdrive ON → Phase C OFF: user-confirmed audible chain engagement. mode 3 mute verified to silence the DAC. **User-confirmed by ear: "mode 1 と同じくクリーン" + "ON で歪んで、OFF でクリーンに戻った"**. `DECISIONS.md` D50. |
| **Pmod I2S2 ADC → AudioLab DSP → DAC (May 20, deployed, branch `feature/pmod-i2s2-dsp-path`, D49)** | **-8.521 ns** | -9195.143 ns | D49 adds `cfg_mode = 2'd2` (ADC → DSP → DAC) and `cfg_mode = 2'd3` (mute) to `pmod_i2s2_master.v`. `pmod_i2s2_integration.tcl` deletes the existing `bclk_1` / `lrclk_1` / `sdata_i_1` block-design nets and re-routes `i2s_to_stream_0`'s clock + serial input onto the Pmod tree (`pmod_master_0/dsp_bclk_o = bclk_int = 3.072 MHz`, `dsp_lrck_o = 48 kHz`, `i2s_to_stream_0/si = ext_pmod_i2s2_ad_sdout_i`). `i2s_to_stream_0/so` keeps driving the legacy ADAU `sdata_o` G18 pin and now also drives `pmod_master_0/dsp_dac_sdin_i`; the DAC SDIN mux inside the master selects between the internal serializer (tone / echo) and the DSP bit-serial output. The existing `axis_data_fifo_0` between `i2s_to_stream_0` and the Clash DSP handles the CDC from Pmod bclk_int (12.288 MHz / 4) to `clk_fpga_0` 100 MHz, exactly as it did before with ADAU bclk. The ADAU `bclk` (R18) / `lrclk` (T17) / `sdata_i` (F17) top-level ports are still constrained but have no internal load any more; ADAU codec stays alive via I2C config so `ADC HPF True` smoke continues to work. WNS regresses by `0.477 ns` vs the mode 1 baseline (`-8.044 ns -> -8.521 ns`), still inside the historical `-7..-9 ns` deploy band and well above the `-9.5 ns` hard gate. WHS `+0.051 ns`, THS `0 ns` (hold remains clean). Utilization after place: Slice LUTs `19490`-class, Slice Registers `22121`-class, Block RAM Tile `9`, DSPs `83` (no change vs mode 1 — the rewiring is wire-only, no new logic). One **new** critical warning: `Common 17-55 'set_property' expects at least one object` on `audio_lab.xdc:2` (`set_property CLOCK_DEDICATED_ROUTE FALSE [get_nets bclk_IBUF]`) because the `bclk_IBUF` net no longer exists when ADAU bclk has no internal load. Harmless but cleanup-worthy. PYNQ-Z2 smoke: `--mode tone --clear` reports MODE=0 + frame_count rising at 48 kHz exactly + ADC peak_abs > 0 with the on-module Line Out ↔ Line In jumper installed. `--mode loopback --confirm-loopback --clear` reports MODE=1 + CLIP_COUNT=0. `--mode dsp --confirm-dsp --clear` reports MODE=2 + frame_count rising + CLIP_COUNT=0. Functional verification: writing `MODE=2` then enabling Overdrive (`set_guitar_effects(overdrive_on=True, overdrive_drive=30)`) raises `peak_abs_left/right` from ~14k (DSP idle / Overdrive off) to ~46k (Overdrive on) over a 1.5 s window with the analog loopback in place -- the DSP chain is processing Pmod ADC samples and the Overdrive gain is observable through the status counters. `MODE=3` (mute) verified to silence the DAC. ADAU HPF True, HDMI VTC GEN_ACTSZ `0x02580320`, encoder VERSION `0x00070001`, pmod_status VERSION `0x00480001`, all 11 effect GPIOs intact. `block_design.tcl`, `GPIO_CONTROL_MAP`, `LowPassFir.hs`, `topEntity`, HDMI integration, encoder PL IP, compact-v2 GUI, notebooks, encoder runtime, ADAU1761 codec init untouched. |
| **Pmod I2S2 PMOD JB external audio (May 19, deployed, branch `feature/pmod-i2s2-bringup`)** | **-8.454 ns** | -8747.137 ns | Phase Pmod-1/2/3 (`DECISIONS.md` D48): retires the Phase 7D close-out PCM5102 / PCM1808 PMOD JB path and assigns PMOD JB exclusively to the Digilent Pmod I2S2 module (CS4344 stereo DAC + CS5343 stereo ADC). `create_project.tcl` unconditionally sources `hw/Pynq-Z2/pmod_i2s2_integration.tcl` (creates `clk_wiz_audio_ext` 100 MHz → 12.288 MHz, instantiates `pmod_i2s2_master` + `axi_pmod_i2s2_status`, adds eight new top-level ports `ext_pmod_i2s2_{da,ad}_{mclk,lrck,sclk}_o` + `ext_pmod_i2s2_{da_sdin_o,ad_sdout_i}`, bumps `ps7_0_axi_periph/NUM_MI` 18 → 19, maps the status slave at `0x43D20000 / 0x10000`). The pin constraints live in a new `audio_lab_pmod_i2s2.xdc` (eight LVCMOS33 entries on `JB1..JB4 / JB7..JB10`); `audio_lab.xdc` keeps only the universal constraints (ADAU + HDMI + encoder). The retired `audio_lab_pcm.xdc`, `pcm5102_dac_integration.tcl`, `pcm1808_adc_integration.tcl`, and the PCM5102 / PCM1808 RTL stay in the repo as archival reference only and are not added to `sources_1`. RTL: `pmod_i2s2_master.v` is an FPGA-master I2S engine (12.288 MHz MCLK → 3.072 MHz BCLK = MCLK/4 → 48 kHz LRCK = BCLK/64, 24-bit MSB-first / 32-bit slot I2S Philips, internal 1 kHz quarter-scale sine ROM for TX, 24-bit deserializer + status counters for RX, modes `cfg_mode=0` TX tone + ADC probe / `cfg_mode=1` ADC → DAC direct loopback); `axi_pmod_i2s2_status.v` exposes VERSION (`0x00480001`) / STATUS / FRAME_COUNT / NONZERO_COUNT / SDOUT_XCOUNT / CLIP_COUNT / LAST_LEFT / LAST_RIGHT / PEAK_ABS_* / MODE / CLEAR. D/A side (JB1/JB2/JB3) and A/D side (JB7/JB8/JB9) are fanouts of one internal `mclk_int` / `bclk_int` / `lrck_int` so they are bit-true synchronous (D40 / D41 async-clocks problem cannot happen here). WNS regresses by `0.523 ns` vs the Phase 7D close-out baseline (`-7.931 ns -> -8.454 ns`), still inside the historical `-7..-9 ns` deploy band and well above the `-9.5 ns` hard deploy gate. Hold remains clean (`WHS = +0.052 ns`, `THS = 0.000 ns`). Utilization after place: Slice LUTs `19490` (`36.64%`), Slice Registers `22121` (`20.79%`), Block RAM Tile `9` (`6.43%`), DSPs `83` (`37.73%`) -- ~+400 LUT / ~+900 register vs Phase 7D, the Pmod I2S2 status block + I2S engine fit comfortably. PYNQ-Z2 deploy completed; `scripts/test_pmod_i2s2.py --duration 5 --clear` reports `frame_count +240281` (matches 48 kHz exactly), `nonzero_count +480561`, `sdout_xcount +4478715`, `peak_abs_left = 242426 / right = 228824` (~`-29 dBFS` of full-scale via the on-module Line Out ↔ Line In 3.5 mm jumper) and PASS. `scripts/pmod_i2s2_capture_probe.py --duration 4 --interval 1.0` reports `48062..48063 frames/sec` (perfect 48 kHz cadence) and PASS. `AudioLabOverlay` still loads, `ADC HPF True`, `v_tc_hdmi GEN_ACTSZ 0x02580320`, encoder `VERSION 0x00070001`, `pmod_status VERSION 0x00480001`. `block_design.tcl`, `GPIO_CONTROL_MAP`, `LowPassFir.hs`, `topEntity`, HDMI integration, encoder PL IP, compact-v2 GUI, notebooks, encoder runtime, and ADAU1761 codec init are all untouched. |
| **PCM1808 mux=ADAU fallback + SCKI on JB8 (May 18, deployed, Phase 7D close-out)** | **-7.931 ns** | -6359.881 ns | Phase 7D close-out (`DECISIONS.md` D42 / D43): `pcm1808_input_select` mux still wires both ADAU `sdata_i` and PCM1808 `ext_adc_dout_i` (JB4 / T10), but the build-time `xlconstant` is set to **`CONST_VAL=0` (ADAU)** so the deployed bit feeds the AXIS DSP chain from the known-good ADAU1761 ADC. PCM1808 hardware diagnosis is deferred (`capture-adc` returned pure 0 on the bench even with smartphone line-in directly on `L_IN/R_IN` and mode straps confirmed at I2S Philips slave; chip is alive enough to clock out frames but does not encode analog input -- suspect damage from the earlier 3.3V-on-`VCC` misconnection that brown-out'd the PMOD rail). To flip back to PCM1808, change `CONFIG.CONST_VAL {0}` to `{1}` in `hw/Pynq-Z2/pcm1808_adc_integration.tcl` and rebuild; no other change needed. PCM1808 SCKI was also moved off JB1 onto a dedicated `ext_pcm1808_sckie_o` port on **JB8 / W16** so JB1 stays constant 0 from `pcm5102_audio_out.v` (`assign ext_audio_mclk_o = 1'b0;`) and the D40 SCK-low guarantee for PCM5102 holds structurally even if a physical wire between JB1 and PCM5102 SCK exists (the first-attempt Phase 7D bit toggled JB1 at 12.288 MHz and re-broke PCM5102 via crosstalk). Output side unchanged from Phase 7E (PCM5102 plays the same bitstream as the ADAU board out). No XDC change vs the Phase 7D first attempt except adding `set_property PACKAGE_PIN W16` for the new SCKI port. **`block_design.tcl` untouched**, **no AXI-Lite slave**, **no new GPIO**, **no GPIO_CONTROL_MAP change**, **no `topEntity` / `LowPassFir.hs` change**, **HDMI / encoder integration untouched**. `clk_fpga_0` 100 MHz domain WNS **improves** by `0.227 ns` vs the Phase 7D first-attempt mux=PCM1808 build (`-8.158 ns -> -7.931 ns`) because the unused PCM1808-branch fanout is pruned by synth when the mux constant is 0. Hold remains clean (`WHS = +0.051 ns`, `THS = 0.000 ns`). `clk_wiz_audio_ext` 12.288 MHz domain is in the design (driving JB8 SCKI) and reports 0 setup / hold violations. `bclk` domain (still drives the PCM5102 BCK fanout and the future PCM1808 BCK fanout) `WNS = +321 ns`-class, 0 violations. Utilization after place: essentially identical to Phase 7E -- Slice LUTs `~19100`, Slice Registers `~21250`, Block RAM Tile `9`, DSPs `83`. PYNQ-Z2 deploy completed; `--inject-sine` plays a clean 1 kHz tone from PCM5102; user-confirmed ADAU Line In -> AudioLab DSP -> PCM5102 line out works on the bench (minor audio-quality nits deferred -- suspect JB7/JB8 crosstalk along the PMOD ribbon, mitigations are wire-length and twisted-pair returns). |
| **PCM1808 ADC input mux (May 18, first attempt, replaced by next row)** | **-8.158 ns** | -6474.516 ns | Phase 7D (`DECISIONS.md` D41): inserts a 2:1 combinational wire mux `hw/ip/pcm1808_adc_input/src/pcm1808_input_select.v` between the existing top-level `sdata_i` port (ADAU1761 ADC, F17) and the new top-level `ext_adc_dout_i` port (PCM1808 DOUT, JB4 / T10) and feeds its output to `i2s_to_stream_0/si`. The mux `sel_external_i` is tied to `1` via an `xlconstant` in `hw/Pynq-Z2/pcm1808_adc_integration.tcl`, so PCM1808 is the build-time-default ADC source; flipping the constant to `0` falls back to ADAU1761 without any other RTL change. `pcm5102_audio_out.v` is reverted from the D40 SCK-low fix (JB1 now carries 12.288 MHz from `clk_wiz_audio_ext` again, but the user's Phase 7D physical board rewiring hard-ties PCM5102 SCK to GND on the module so this no longer affects PCM5102 -- the wizard output is reused as PCM1808 SCKI). The 12.288 MHz domain (`clk_out1_block_design_clk_wiz_audio_ext_0`) is back in the design (was pruned in D40) and reports 0 setup/hold violations; the `bclk` domain (now driving PCM1808 BCK fanout too) holds `WNS = +321.256 ns`. `block_design.tcl` untouched directly; integration tcl deletes the existing `sdata_i_1` net and re-routes through the mux. **No AXI-Lite slave**, **no new GPIO**, **no GPIO_CONTROL_MAP change**, **no `topEntity` / `LowPassFir.hs` change**, **HDMI / encoder integration untouched**, **PCM5102 DSP output path (D39) untouched**. `clk_fpga_0` 100 MHz domain WNS regresses by `0.154 ns` vs the D40 SCK-low build (`-8.004 ns -> -8.158 ns`) -- the wizard is back AND the new mux + xlconstant add a touch of fanout -- still inside the historical `-7..-9 ns` deploy band. Hold remains clean (`WHS = +0.051 ns`, `THS = 0.000 ns`). Utilization after place: Slice LUTs `19099` (`35.90%`), Slice Registers `21253` (`19.97%`), Block RAM Tile `9` (`6.43%`), DSPs `83` (`37.73%`) -- essentially identical to Phase 7E. PYNQ-Z2 deploy completed; `scripts/test_pcm1808_adc_to_pcm5102.py --duration 3` PASS (overlay loads, every required IP intact, ADC HPF True, `EncoderGuiSmoke.ipynb` / `Pcm5102DspOutputCheck.ipynb` still 1-cell on board). Acoustic test (PCM1808 line in -> AudioLab DSP -> PCM5102 line out) is the next manual step. **Known async-clocks caveat:** PCM1808 SCKI is NOT bit-true synchronous to ADAU-PLL-sourced BCK; PCM1808 lacks a PCM510x-style internal-PLL fallback, so the bench may reveal the same kind of graininess Phase 7E showed before D40 -- in that case the next escalation is FPGA-as-I2S-master, not another RTL tweak. |
| **PCM5102 SCK tied LOW (May 18, deployed)** | **-8.004 ns** | -6767.334 ns | Phase 7E follow-up (`DECISIONS.md` D40): `pcm5102_audio_out.v` now drives `ext_audio_mclk_o = 1'b0` instead of the 12.288 MHz wizard output. PCM510x enters internal-SYSCLK mode (BCK-derived sysclk) and the audible graininess / periodic jitter from the May-18 morning `9f21546` build disappears (the 12.288 MHz from PS-PLL was not bit-true synchronous to the ADAU1761-PLL-sourced BCK, so external-SCK locking drifted in and out). The `clk_wiz_audio_ext` wizard is *kept in the integration tcl* for future PCM1808 SCKI use but its output has no consumer in this bit and synth prunes the entire 12.288 MHz domain; WNS therefore **improves** by `+0.720 ns` vs the earlier Phase 7E build (`-8.724 ns -> -8.004 ns`), inside the historical `-7..-9 ns` deploy band. ADAU1761 / DSP chain / HDMI / encoder / GPIO_CONTROL_MAP all untouched. PCM1808 still NOT implemented. Hold remains clean (`WHS = +0.052 ns`, `THS = 0.000 ns`). Utilization after place is essentially the Phase 7E figure minus the wizard / clock buffers. PYNQ-Z2 deploy + smoke PASS. Acoustic re-test by the user on the bench: pending. |
| **PCM5102 DSP-output ADAU-mirror (May 18, deployed)** | **-8.724 ns** | -7029.676 ns | Phase 7E: the Phase 7C free-running tone module `pcm5102_dac_tone` is no longer instantiated. The new RTL `hw/ip/pcm5102_audio_out/src/pcm5102_audio_out.v` is a trivial 4-signal pass-through that mirrors the ADAU1761 I2S DAC interface onto PMOD JB: BCK <- top-level `bclk` input port (R18), LCK <- top-level `lrclk` input port (T17), DIN <- `i2s_to_stream_0/so` (same serial DAC bitstream the ADAU `sdata_o` pin G18 receives), MCLK <- `clk_wiz_audio_ext/clk_out1` 12.288 MHz (Phase 7C MMCM kept intact). PCM5102 now carries bit-for-bit the same processed audio the ADAU1761 DAC receives; both DACs stream in parallel. **No AXIS / FIFO / CDC / SRC was added** -- the new fanouts simply add sinks on the existing nets. **`block_design.tcl` is untouched directly**; the instance swap is done in `hw/Pynq-Z2/pcm5102_dac_integration.tcl`. **No XDC change** (Phase 7C pin assignments unchanged). **No AXI-Lite slave**, **no new GPIO**, **no `GPIO_CONTROL_MAP.md` change**, **no `topEntity` / `LowPassFir.hs` change**, **HDMI / encoder integration untouched**, **PCM1808 ADC still NOT implemented (Phase 7D pending).** Per-clock timing: `bclk` domain (3.072 MHz from ADAU, now also drives PCM5102 BCK fanout) `WNS = +320.751 ns`, 0 setup or hold violations. `clk_fpga_0` 100 MHz domain WNS `-8.724 ns` regresses by `0.314 ns` vs Phase 7C (`-8.410 ns`), still inside the historical `-7..-9 ns` deploy band. Hold remains clean (`WHS = +0.051 ns`, `THS = 0.000 ns`). Utilization after place: Slice LUTs `19102` (`35.91%`), Slice Registers `21255` (`19.98%`), Block RAM Tile `9` (`6.43%`), DSPs `83` (`37.73%`) -- essentially identical to Phase 7C. PYNQ-Z2 deploy completed; `scripts/test_pcm5102_dsp_output.py --duration 3` PASS (overlay loaded, ADC HPF True, every required IP intact). Acoustic A/B comparison between ADAU board out and PCM5102 line out is the next manual step. `DECISIONS.md` D39. |
| **Selectable Overdrive model build (May 18, deployed, D46)** | **-8.105 ns** | -6643.894 ns | D46: generic Overdrive retired and replaced by six selectable models (TS9 / OD-1 / BD-2 / Jan Ray / OCD / CENTAUR). Implementation is constant-coefficient tables (`odDriveK / odKneeP / odKneeN / odSafetyKnee` in `AudioLab/Effects/Overdrive.hs`) selected by a 3-bit `overdriveModel` field that lives in `axi_gpio_overdrive.ctrlD[2:0]` -- the same byte as `distTight` (top 5 bits), because every Clash consumer of `distTight` shifts right by 3 or 4 and already discards the low 3 bits. No new register stage, no new multiplier, no `topEntity` port change, no `block_design.tcl` change, no new GPIO. WNS regresses by `0.174 ns` vs the Phase 7D close-out baseline (`-7.931 ns -> -8.105 ns`); still inside the historical `-7..-9 ns` deploy band and well clear of the rejected May 4 -15.067 ns `model_select` shape. Hold remains clean (`WHS = +0.044 ns`, `THS = 0.000 ns`). Utilization after place: Slice LUTs `19159` (`36.01%`), Slice Registers `21287` (`20.01%`), Block RAM Tile `9` (`6.43%`), DSPs `83` (`37.73%`); essentially identical to the Phase 7D close-out figure (delta of a handful of LUT/REG from the new constant LUT muxes). `ADAU1761` Clash IP was re-generated to bitwise-identical VHDL during the rebuild. PYNQ deploy + on-board smoke pending. |
| **PCM5102 DAC-only bring-up (May 18, deployed)** | **-8.410 ns** | -7313.564 ns | Phase 7C: adds the free-running PCM5102 DAC tone path on PMOD JB. New RTL `hw/ip/pcm5102_dac_tone/src/pcm5102_dac_tone.v` is a Verilog I2S master that divides MCLK by 4 (BCLK) and BCLK by 64 (LRCLK) and shifts a 48-entry / 24-bit quarter-scale sine ROM out as I2S Philips 32-bit slots. A new MMCM `clk_wiz_audio_ext` turns FCLK_CLK0 100 MHz into **12.288 MHz exact** via `DIVCLK_DIVIDE=5, MULT_F=48.0, CLKOUT0_DIVIDE_F=78.125, VCO=960 MHz`. Integration tcl `hw/Pynq-Z2/pcm5102_dac_integration.tcl` is sourced from `create_project.tcl` after `encoder_integration.tcl`. **No AXI-Lite slave**, **no new GPIO**, **no `block_design.tcl` direct edit**. XDC adds four LVCMOS33 outputs: `ext_audio_mclk_o W14 (JB1)`, `ext_audio_bclk_o Y14 (JB2)`, `ext_audio_lrclk_o T11 (JB3)`, `ext_dac_din_o V16 (JB7)`; no PULLUP. ADAU1761 path / HDMI integration / encoder integration / GPIO_CONTROL_MAP / LowPassFir DSP all untouched. **PCM1808 ADC is NOT added (Phase 7D).** The new 12.288 MHz domain (`clk_out1_block_design_clk_wiz_audio_ext_0`) reports WNS `+77.576 ns`, 0 setup or hold violations -- the new path is trivially met. The full-design WNS `-8.410 ns` lives on `clk_fpga_0` 100 MHz, the same failing domain as the existing baseline; it regresses by `0.314 ns` vs Phase 6I C2 (`-8.096 ns`) and stays inside the historical `-7..-9 ns` deploy band. Hold remains clean (`WHS = +0.052 ns`, `THS = 0.000 ns`). Utilization after place: Slice LUTs `19145` (`35.99%`), Slice Registers `21293` (`20.01%`), Block RAM Tile `9` (`6.43%`), DSPs `83` (`37.73%`). PYNQ-Z2 deploy completed; `scripts/test_pcm5102_dac_tone.py --duration 3` PASS (overlay loaded, ADAU1761 DMA / distortion GPIO / encoder `enc_in_0/s_axi` / HDMI VDMA / HDMI VTC all present; `pcm5102_dac_0` and `clk_wiz_audio_ext` correctly absent from `ip_dict` because they expose no AXI registers). Acoustic verification on PCM5102 line out is the next manual step. `DECISIONS.md` D38. |

The current accepted baseline is D112: overall WNS `+0.564 ns`, bit md5
`c1e3de50dca946c24b1b08106f8f134c`, bench-accepted on Pmod mode 2. D119
(`88c265cc`) is the current built/file-synced Amp volume-stability candidate
with overall WNS `+0.699 ns`, but it is not confirmed loaded, not PL-smoked, and
not accepted until the board is reachable again and the user confirms
safe-bypass plus Amp volume stability on the bench. D118 (`c85ada77`) remains
the last newer candidate that was PL-smoked. Older accepted rollback points
include D98 (`18df313f`) and D79 (`f0cb0276`). Treat any timing slip, D109 CDC
constraint regression, or audible bypass-path artifact as a regression unless
the tradeoff is explicit and bench-accepted.

**Revised engineering rule (D58 / D59 / D60 / D61 v2 / D63 / D64 all
rejected, D62 accepted, ordered by structural distance from D62
"safe" pattern)**:

| Class | Trigger | Example | Result |
| ----- | ------- | ------- | ------ |
| Obvious structural | New DSP48E1 multiplier(s) or new `Pipeline.hs` register stage | D58 (DSP 83->87), D59 (full-Frame carry pipeline), D61 v1 (DSP 83->88), D61 v2 (2 new feedback Sample registers) | rejected |
| Less obvious structural | New helper invocation inside existing stage; helper swap; helper cascade | D63 (`asymSoftClip` -> `asymHardClip` cascade in `ds1ClipFrame`, 0 new DSP, 0 new register) | rejected |
| Subtle structural | "Pure constants only" but at multi-model / multi-constant scope | D64 (5 constants across 3 distortion pedals' existing `asymSoftClip` invocations) | rejected |
| Reliably safe | "Pure constants only" at *minimal* scope (one model, <=3 constants) | D62 (3 constants in one Overdrive model `Overdrive.hs`), D66 (2 constants in one Distortion pedal), D67 (3 constants in one Amp model) | accepted |

**Adding DSP48E1 multipliers OR new feedback state registers OR new helper
invocations OR multi-pedal coefficient retunes -- even when DSP / BRAM /
register count and headline WNS look fine -- can perturb Vivado P&R into
leaking HF noise onto the safe-bypass path and sometimes onto other effect
paths sharing the same axis_switch.** D78 adds the related lesson that even
an additive AXI master can perturb the DSP-island placement enough to
bitcrush; phys_opt recovered it. D79 adds the arithmetic-shape lesson:
parallel multiplies can route better than a "simpler" serial LERP on this
island. The bench ear on safe-bypass plus an A/B against the prior baseline
on every touched model remains the only sensor that has caught this
regression class reliably; macroscopic timing summary, CLIP_COUNT,
FRAME_COUNT, DSP / BRAM / register counts, GUI smoke, and programmatic
deploy-time smoke are necessary but not sufficient.

Phase 4C parsed the existing routed reports directly where available:
`block_design_wrapper_timing_summary_routed.rpt`,
`block_design_wrapper_utilization_placed.rpt`, and
`block_design_wrapper_clock_utilization_routed.rpt`. The HDMI
pixel/serial domains are not the limiting paths: the HDMI pixel clock
domain has WNS `+2.622 ns`, `PixelClkIO` pulse-width slack is
`+11.313 ns`, and `SerialClkIO` pulse-width slack is `+0.538 ns`.
The remaining negative setup slack is still associated with the
pre-existing audio-side timing, not the HDMI output clocks.

## Why the `model_select` attempt regressed timing

The first refactor put eight parallel computations behind a single
`case modelSelect of …` in every distortion stage:

- `distModelClipFrame` had eight different clip variants, each with its
  own knee/threshold arithmetic, fed into one final mux.
- `distModelPostFilterFrame` computed `lp`, `hp`, and `blend` in
  parallel and selected on `modelSelect`.
- `modelPreGain` returned a different `Unsigned 12` per model, yielding
  an 8-way mux feeding a 24×12 multiply.

Each of those builds a tall combinational tree per stage. Even with
register stages between, the **per-stage** depth blew through the 10 ns
clock window, pushing WNS from −7.7 ns to −15.1 ns.

## Rules of thumb that hold for this design

- A single `case` over a small enum is fine **inside a register stage**
  if the case body is cheap (a constant lookup, a conditional add).
  Do not put a multiply or a clip behind a wide case.
- Multipliers (`mulU8`, `mulU12`, `mulS10`) are DSP48 hard blocks and
  pipeline well, but their inputs and outputs need their own register
  stages in this design. Don't chain `mulU12 -> case -> hardClip` in
  one combinational block.
- One-pole IIR filters (`onePoleU8`) take one stage's worth of depth
  on their own. Keep them in their own register stage when possible.
- BRAM-backed delays (e.g. the reverb tap) should not have their
  address path cross a model selector — the address is needed early
  and any extra fanout makes the read-data path tighter.

## Deploy gate

A bitstream may be deployed only if the Vivado run prints
`write_bitstream completed successfully`, the final WNS is no worse than
the previous deployed build by an audibly meaningful margin, the 100 MHz
audio fabric remains clean, and the bench safe-bypass check does not
reveal bitcrusher / HF bypass artifacts. The latest accepted baseline is D112
(`c1e3de50`, overall WNS `+0.564 ns`); D119 (`88c265cc`, overall WNS
`+0.699 ns`) is built and file-synced but still needs PL smoke and bench
acceptance after the board is reachable again. If timing slips significantly,
the D109 `clk_fpga_0`<->`clk` CDC constraints are weakened, or safe-bypass gets
noisier, the change must be revisited (more pipeline stages, simpler mux
structure, fewer features, or narrower retune scope) before adoption.

When adding a new pedal or filter stage:

- Keep each pedal as its own register-staged block. Reuse the
  shape of `clean_boost`, `tube_screamer`, or `metal`.
- **Do not** add a single function with a wide `case` selecting
  between independent multipliers / clippers / filters. That is the
  pattern that caused the -15.067 ns regression.

When timing is significantly worse, the user-visible failure modes are
typically:

- Audio glitches that come and go with PVT and routing decisions.
- Occasional wrong sample values, perceived as crackle or DC pops.
- In the worst case, BRAM corruption (this design has BRAM-backed
  reverb taps).

These do not show up in passthrough mode but appear once an effect that
exercises a slow path is enabled, which makes them hard to debug from
inside Jupyter.

## Timing candidate: Compressor + Cab pipeline split v2

Branch: `timing/comp-cab-split-v2`
Commit: `24fa72b`

This candidate keeps the audio voicing constants unchanged and only
splits timing-heavy stages:

- Compressor target-gain calculation and gain smoothing are separated
  using `Maybe CompTarget`. `Nothing` cycles hold the previous
  compressor gain and do not reset gain to unity.
- Cab products and cab saturation / softClip processing are separated
  by `cabSatPipe`.
- No new AXI GPIO, no block design change, no DSP48 increase, no BRAM
  increase. `Makefile` remains environment-independent.

Timing comparison (all clean / non-incremental builds):

| Build | WNS | TNS | Notes |
| --- | ---: | ---: | --- |
| D71 clean default | -10.854 ns | -11045 ns | Fresh P&R baseline |
| Comp+Cab v2 default | -10.687 ns | -10314 ns | WNS +0.167 ns, TNS +731 ns |
| Comp+Cab v2 Performance_Explore | -9.972 ns | -10138 ns | WNS +0.882 ns vs D71 clean |

Top-100 worst-path distribution:

| Source | D71 clean | Comp+Cab v2 Explore |
| --- | ---: | ---: |
| Compressor | 23 | 0 |
| Amp | 28 | 0 |
| DS-1 | 24 | 76 |

Conclusion: Comp+Cab v2 is a timing candidate and should be kept on the
branch, but it is not timing-closed and should not be merged to main
until bench checks are complete.

## Rejected DS-1 split attempt

A DS-1 timing split was attempted by separating `satShift8(fAccL)` from
`asymSoftClip` using a new `ds1BoostFrame` stage in `Distortion.hs` and
a `ds1BoostPipe` register in `Pipeline.hs`.

| Build | WNS |
| --- | ---: |
| Comp+Cab v2 default | -10.687 ns |
| Comp+Cab+DS1 split default | -11.378 ns |

The DS-1 split degraded WNS by about 0.691 ns relative to Comp+Cab v2
default. The code was reverted and is not committed.

Conclusion: do not retry this exact `ds1BoostFrame -> ds1ClipFrame`
split. The DS-1 bottleneck appears to be dominated by routing / `Maybe
Frame` mux / CARRY4 / fanout pressure rather than only local
`satShift8 + asymSoftClip` combinational depth.
